# CLAUDE.md — Operational Memory for Claude Code

> Этот файл читается Claude Code автоматически в начале каждой сессии в проекте `ege-app`. Он содержит контекст, конвенции и правила, обязательные для соблюдения.

---

## Контекст пользователя (read-once, держи в голове)

Пользователь — 18-летний студент в Германии, готовится к ЕГЭ-2027 (русский язык + профильная математика). Цель: высокий балл для поступления на специалиста по информационной безопасности. Дополнительно — спор с родителями, что сдаст ЕГЭ.

- Python: базовый уровень — понимает код, делает мелкие правки, **не** пишет с нуля сложные системы.
- Kotlin / Compose: с нуля. Объясняй технические решения коротко и по делу, но не пропускай совсем.
- Время разработки: длинный горизонт (~13 месяцев до ЕГЭ). Это создаёт риск размытия мотивации — см. Safety Rules.

Когда сомневаешься — **спрашивай пользователя, не угадывай**. Особенно перед: bulk-сетевыми запросами, удалением файлов, сменой архитектуры.

---

## Проект: что строим

Личное Android-приложение для подготовки к ЕГЭ. **Два** предмета: профильная математика, русский. **Не** Google Play, **не** общедоступный продукт, **не** мульти-устройство.

Архитектура — два независимых подпроекта:

```
C:\Projects\ege-app\
├── CLAUDE.md                        ← этот файл
├── PROJECT_OVERVIEW.md              ← для будущих чатов в claude.ai (не трогать)
├── PHASE_N_PROMPT.md                ← инструкции текущей фазы
│
├── parser/                          (Python 3.13)
│   ├── fipi-recon-archive/          ← запасной парашют (НЕ удалять)
│   ├── scrapers/                    ← создашь в Фазе 1
│   ├── pipeline/                    ← fetch/parse/normalize/store
│   ├── cache/raw/                   ← idempotent кеш сырого HTML
│   ├── assets/                      ← скачанные картинки
│   ├── state.json                   ← чекпоинт парсера (last_seen, errors)
│   ├── selectors.yaml               ← CSS-селекторы вынесены, чтобы быстро чинить
│   ├── math.jsonl                   ← нормализованные задачи математики
│   ├── russian.jsonl                ← нормализованные задачи русского
│   ├── kim-fipi/                    ← открытые варианты КИМ ФИПИ (Stage 4)
│   ├── build_db.py                  ← JSONL → corpus.db
│   ├── corpus.db                    ← финальная SQLite БД
│   └── requirements.txt
│
└── android/                         (создаётся в Фазе 2)
    └── (стандартный Android Studio проект)
```

---

## Tech stack — обязательный

| Слой | Технология |
|---|---|
| Парсер | Python 3.13 + httpx + selectolax + PyYAML |
| Запасной парсер | Playwright (только если sdamgia включит Cloudflare) |
| Промежуточный формат | JSONL + папка `assets/` |
| БД | SQLite (на Android — через Room) |
| Mobile UI | Kotlin + Jetpack Compose |
| Рендер формул | **AsyncImage / Coil** — sdamgia отдаёт формулы как готовые SVG-картинки с контент-адресуемым URL (`/formula/svg/{2hex}/{32hex}.svg`) и alt-текстом на русском. MathJax и WebView **не нужны** — это пересмотрено по итогам Stage 0 разведки (см. секцию «Tech stack — заметки после Stage 0»). |
| HTTP в Android | Ktor Client |
| AI | Claude API напрямую, модели: `claude-haiku-4-5-20251001` по умолчанию, `claude-sonnet-4-7` по запросу |

---

## Схема БД — трёхуровневая иерархия задач

**Критично:** структура задач должна поддерживать радар по подвидам (Safety Rule #1).

```sql
CREATE TABLE subjects (
    id INTEGER PRIMARY KEY,
    slug TEXT NOT NULL UNIQUE,           -- 'mathb', 'rus'
    title TEXT NOT NULL,
    sdamgia_subdomain TEXT NOT NULL
);

CREATE TABLE problem_types (             -- ЕГЭ-номера: №1, №2, ..., №19, плюс «Дополнительные Д1..Д19»
    id INTEGER PRIMARY KEY,
    subject_id INTEGER NOT NULL REFERENCES subjects(id),
    number INTEGER NOT NULL,             -- 1..N для основных, 1..19 для supplementary математики
    title TEXT NOT NULL,                 -- «Планиметрия», «Чтение графиков и диаграмм», …
    description TEXT,
    is_supplementary INTEGER NOT NULL DEFAULT 0,  -- 0 = ЕГЭ-номер, 1 = «Задания Д1..Д19»
    UNIQUE(subject_id, number, title, is_supplementary)  -- title в ключе: у математики 2 разных типа с number=10 («C2. Сложная стереометрия» и «Графики функций»)
);

CREATE TABLE problem_subtypes (          -- темы КЭС: тригонометрия, показательные, ...
    id INTEGER PRIMARY KEY,
    type_id INTEGER NOT NULL REFERENCES problem_types(id),
    kes_code TEXT,                       -- '1.5' и т.д., если sdamgia/ФИПИ дают
    title TEXT NOT NULL,
    UNIQUE(type_id, title)
);

CREATE TABLE problems (
    id INTEGER PRIMARY KEY,
    subject_id INTEGER NOT NULL REFERENCES subjects(id),  -- денормализация для быстрого фильтра + UNIQUE-ключ
    sdamgia_id TEXT NOT NULL,
    prototype_id TEXT,                   -- если sdamgia группирует по прототипам
    type_id INTEGER NOT NULL REFERENCES problem_types(id),
    subtype_id INTEGER REFERENCES problem_subtypes(id),
    statement_html TEXT NOT NULL,
    answer TEXT,
    answer_format TEXT,                  -- 'number' | 'string' | 'multipart' | 'alternatives' | NULL
    images_json TEXT,                    -- JSON-массив относительных путей
    source TEXT,                         -- «ОБЗ ФИПИ», «РЕШУ ЕГЭ» — для русского из блока align-left. Для математики NULL (тематика — через subtype).
    difficulty TEXT,                     -- «обычная», «повышенная» — только для русского. Для математики NULL.
    scraped_at TEXT NOT NULL,
    raw_hash TEXT NOT NULL,
    UNIQUE(subject_id, sdamgia_id)       -- sdamgia использует общий ID-пул для разных предметов: на Stage 3 обнаружено 9 коллизий с math (низкие ID 902-915 и др., историческое наследие). Поэтому уникальность по паре.
);

CREATE TABLE solutions (
    problem_id INTEGER PRIMARY KEY REFERENCES problems(id),
    solution_html TEXT NOT NULL,
    explanation_text TEXT                -- plain-text для скармливания в Claude
);

CREATE TABLE user_progress (
    problem_id INTEGER PRIMARY KEY REFERENCES problems(id),
    status TEXT NOT NULL,                -- 'not_started' | 'attempted' | 'correct' | 'wrong' | 'reviewed'
    user_answer TEXT,
    attempts INTEGER DEFAULT 0,
    last_attempt_at TEXT,
    flagged INTEGER DEFAULT 0,
    used_ai INTEGER DEFAULT 0            -- если жал «Спросить ИИ» хотя бы раз
);

CREATE TABLE ai_conversations (
    id INTEGER PRIMARY KEY,
    problem_id INTEGER NOT NULL REFERENCES problems(id),
    user_question TEXT NOT NULL,
    ai_response TEXT NOT NULL,
    prompt_hash TEXT NOT NULL UNIQUE,    -- sha256(problem_id + normalize(question))
    model TEXT NOT NULL,
    tokens_in INTEGER,
    tokens_out INTEGER,
    cost_usd REAL,
    created_at TEXT NOT NULL
);

CREATE TABLE error_atoms (               -- Фаза 5: SRS по ловушкам
    id INTEGER PRIMARY KEY,
    title TEXT NOT NULL,                 -- 'Забыл ОДЗ', 'Перепутал sin/cos', ...
    description TEXT,
    subject_id INTEGER NOT NULL REFERENCES subjects(id),
    related_subtype_ids TEXT,            -- JSON-массив
    next_review_at TEXT,
    review_interval_days INTEGER DEFAULT 1,
    times_failed INTEGER DEFAULT 0
);

CREATE TABLE mock_exams (
    id INTEGER PRIMARY KEY,
    subject_id INTEGER NOT NULL REFERENCES subjects(id),
    source TEXT NOT NULL,                -- 'generated' | 'fipi-official'
    started_at TEXT,
    completed_at TEXT,
    raw_score INTEGER,                   -- первичный балл
    scaled_score INTEGER,                -- вторичный балл
    problem_ids_json TEXT NOT NULL,      -- список задач в варианте
    answers_json TEXT                    -- ответы пользователя
);

CREATE TABLE daily_streak (              -- счётчик дней подряд
    date TEXT PRIMARY KEY,               -- YYYY-MM-DD
    problems_solved INTEGER NOT NULL,
    streak_value INTEGER NOT NULL
);

-- Справочные правила (для русского — извлекаются из sdamgia,
-- для математики — пишутся вручную, см. примечание ниже).
CREATE TABLE rules (
    id INTEGER PRIMARY KEY,
    subject_id INTEGER NOT NULL REFERENCES subjects(id),
    title TEXT NOT NULL,                 -- «Союзы противопоставления», «Согласные в корне»
    content_html TEXT NOT NULL,          -- полный HTML блока «Правило» с тегами
    source TEXT,                         -- 'sdamgia' | 'user-curated'
    rule_hash TEXT NOT NULL UNIQUE       -- sha256(нормализованный_text) — для дедупликации
);

-- Связь many-to-many: одно правило часто прикреплено к десяткам задач,
-- одна задача может ссылаться на 0..N правил (обычно 1).
CREATE TABLE problem_rules (
    problem_id INTEGER NOT NULL REFERENCES problems(id),
    rule_id INTEGER NOT NULL REFERENCES rules(id),
    PRIMARY KEY (problem_id, rule_id)
);
CREATE INDEX idx_problem_rules_rule ON problem_rules(rule_id);

CREATE VIRTUAL TABLE problems_fts USING fts5(
    statement_html, content=problems, content_rowid=id
);
```

**Правила: разделение русского и математики.**
- **Русский:** правила извлекаются автоматически из блока «Правило» (скрытый `align-left > div span[font-weight:bold]` + соседний `div.pbody`) на каждой задаче. Скрипт `parser/scrapers/extract_rules.py` пост-процессит уже скачанные HTML в `cache/raw/`, дедуплицирует по `rule_hash`, заполняет `rules` и `problem_rules`. `rules.source = 'sdamgia'`.
- **Математика:** правила НЕ парсятся из sdamgia. У пользователя есть свои правила (рукописные/из учебников), которые будут добавлены вручную в `rules` с `source = 'user-curated'` на Фазе 3+. У задач математики поле «правило» в Android-UI скрыто пока в `problem_rules` нет записей для этой задачи.

---

## Конвенции парсера (обязательные)

1. **Rate limiting:** 1 запрос в 1.5-2 секунды + `random.uniform(0.5, 1.0)` джиттер. На 429/503 — экспоненциальный бэк-офф (1s, 2s, 4s, 8s, ...).
2. **User-Agent:** реальный десктоп Chrome, не «python-httpx».
3. **Чекпоинты:** каждые 50 успешных задач сохранять `state.json` со списком обработанных ID и временем. На рестарте — продолжать с места.
4. **Idempotent cache:** каждый успешно скачанный HTML кешировать в `cache/raw/{sdamgia_id}.html`. Парсинг повторно — без повторного сетевого запроса.
5. **Селекторы в `selectors.yaml`:** все CSS-селекторы вынесены. Это нужно чтобы починить парсер при изменении разметки sdamgia за минуты, а не часы.
6. **Картинки:** скачивать в `assets/{sdamgia_id}/img_N.{ext}`, в JSONL хранить только относительные пути.
7. **MathML:** не пытаться конвертировать в LaTeX. Хранить как есть, рендерить MathJax-ом в WebView на Android.
8. **Smoke-тесты:** в `parser/tests/smoke_selectors.py` хранить N эталонных задач с ожидаемыми полями. Прогонять перед каждым обновлением парсера и после любого изменения `selectors.yaml`.
9. **Запуск парсера в background — только через Claude Code `run_in_background=true`.** Bash `&` оставляет процесс-зомби после выхода shell (на Windows процесс не получает SIGHUP сразу) — параллельный запуск через `&` И `run_in_background` уже один раз дал 12 дубликатов в `math.jsonl` (см. инцидент 2026-05-16, исправлено dedup-скриптом). При resume всегда: проверить `Get-CimInstance Win32_Process -Filter Name="python.exe"` на висящие процессы ДО запуска нового.
10. **Угадывание расширения файла по URL — НЕЛЬЗЯ для иллюстраций sdamgia.** Для URL вида `https://math-ege.sdamgia.ru/get_file?id=N` в пути нет расширения, а Content-Type sdamgia не возвращает в стабильном виде. Старый normalize.py использовал fallback `.png` → 3128 из 3510 файлов оказались SVG под именем `.png` (см. инцидент 2026-05-17). Лечение пост-фактум: `parser/scrapers/fix_illustration_extensions.py` (читает magic bytes, переименовывает + точечно UPDATE'ит corpus.db). На будущее (при добавлении новых иллюстраций): определять реальный тип по magic bytes после скачивания, а не по URL.
11. **Inline `display:none` в HTML sdamgia.** Решения, правила и некоторые блоки задач отдаются с `style="...;display:none"` — на самом sdamgia они раскрываются собственным JS, которого у нас нет. При рендере HTML в любом нашем UI (view_corpus.html, Android WebView) **обязательно** прогонять через sanitize-функцию, которая удаляет декларацию `display:none` из inline styles. Эталонная реализация — `strip_display_none()` в `parser/export_view.py`.
12. **DDL для corpus.db — PRIMARY KEY на уровне таблицы, не inline.** SQLite-инструкция `id INTEGER PRIMARY KEY` (inline, без явного `NOT NULL`) делает колонку rowid-alias, и `PRAGMA table_info` возвращает `notnull=0`. Room (Android, начиная с 2.x) генерит из `@PrimaryKey val id: Long` форму `id INTEGER NOT NULL, ..., PRIMARY KEY(id)` (`notnull=1`) и сравнивает `Column.notNull` через `equals` напрямую — без скидки на PK-колонку. На pre-packaged-БД через `Room.createFromAsset` это приводит к `IllegalStateException: Pre-packaged database has an invalid schema` сразу при старте приложения (см. инцидент 2026-05-17). Правило: для всех таблиц, которые планируется маппить через Room-сущности (или которые могут быть замаплены в будущем — фактически все, кроме virtual FTS), писать `col TYPE NOT NULL, ..., PRIMARY KEY(col)`. Композитные PK (как `problem_rules`) уже table-level, для них правило выполняется автоматически.
13. **HTML refs в corpus.db должны соответствовать реальным файлам на диске.** После каждой пересборки `corpus.db` через `build_db.py` обязательно прогонять `parser/scrapers/fix_html_image_refs.py`. Он чинит расхождения которые остаются в JSONL (например `src="27238/img_1.png"` когда на диске `.svg`), переводит абсолютные URL формул (`https://...sdamgia.ru/formula/svg/XX/HASH.svg`) в локальный путь `_formulas/XX/HASH.svg`, удаляет 28k+ UI-иконок sdamgia (`/img/exclamation.png`, `chain.png`, `printer.png`, `expand/collapse.jpg`), и точечно UPDATE'ит `images_json` где может. Без этого Android вообще НЕ показывает чертежи (см. инцидент Stage 3 polish 2). Старый `fix_illustration_extensions.py` трогает только имена файлов на диске, его одного **недостаточно** — нужны оба, и в правильном порядке: сначала extensions, потом html_image_refs.
14. **Luminance inversion для SVG в тёмной теме.** Sdamgia рисует формулы и геометрические чертежи чёрным по белому. На чёрном фоне приложения они невидимы. ColorMatrix-инверсия (полная RGB) ломает цветные элементы — оранжевая окружность становится синеватой. Правильное решение: **попиксельный обход** в `HtmlRenderer.applyLuminanceInversion`, для каждого пикселя считаем `maxDiff = max(|R-G|,|G-B|,|R-B|)`. Если `maxDiff < 30` (т.е. пиксель серый/чёрный/белый) — инвертируем RGB; иначе оставляем как есть. Это даёт «чёрные линии → белые, оранжевая окружность остаётся оранжевой». Производительность приемлемая: ~3K пикселей для inline-формулы, до 780K для большого чертежа (≤10мс на Snapdragon, идёт на `Dispatchers.IO`). Применяется ко **всем** SVG/PNG (формулам и иллюстрациям) единообразно — `_formulas/` и `<sid>/img_N.<ext>`.
15. **UI-система — единый набор Apple-style примитивов.** Любой новый экран использует:
    - `AppleButton.kt` — `PrimaryButton` (52dp, SystemBlue, corner 16dp), `SecondaryButton` (44dp, tinted, corner 14dp), `TertiaryButton` (borderless). Все через общий `PressableSurface` с scale 0.97 spring + haptic LongPress.
    - `AppleTextField.kt` — `IosTextField` (BgElevated2, corner 14dp, cursor SystemBlue, без OutlinedTextField-обводки).
    - `AppleCard.kt` — `AppleCard` (corner 24dp, padding 22dp, BgElevated, scale 0.985 при нажатии), `AppleListRow` (icon-в-круге + title + subtitle + ›).
    - `Scaffolds.kt` — `LargeTitleBar` (34sp Bold заголовок + 15sp subtitle + 48dp back-button), `SmallTitleBar`.
    - `Color.kt` — палитра (Bg/BgElevated/BgElevated2, Label/LabelSecondary/LabelTertiary, SystemBlue/Green/Red/Orange/Yellow + Tint).
    - `Type.kt` — Inter через Google Fonts (`com_google_android_gms_fonts_certs` в `res/values/fonts_certs.xml`).
    Не создавать собственные `Button`/`Card`/`TextField` для UI — это нарушает iOS-look и плодит несогласованность.
16. **`readSvgSize` для определения естественного размера формул.** Без чтения SVG viewBox простая формула `√3` рендерилась block-картинкой высотой 360dp (вылезала за экран). Правило: при рендере `<img class="tex">` в block-режиме обязательно прочитать `readSvgSize(context, assetPath)` — первые 1KB SVG, regex на viewBox или width/height. Затем `BlockFormula` использует `min(naturalHeight, 120dp)` и **wrapContentSize** (НЕ fillMaxWidth). Иллюстрации (`<sid>/img_N.<ext>`) рендерятся отдельной функцией `BlockIllustration` с `fillMaxWidth + heightIn(max=360dp) + border + surface bg`. Решение принято в Stage 4 Polish 4.
17. **Тренажёры русского — единый паттерн.** Любой новый тренажёр (тип ЕГЭ с тренируемыми словами / формами / правилами) следует структуре:
    - **Источник данных** — JSON в `android/app/src/main/assets/<name>.json` (gitignored, исходник в репо).
    - **Repository** — `object <Name>Repository` с `load(context)` (in-memory cache + ленивая инициализация).
    - **ErrorsStore** — `DataStore<Preferences>` отдельный (по `preferencesDataStore("<name>_errors")`). Запись ошибок в `viewModelScope.launch` при `Verdict.isRight == false`. UI-режим «только ошибки» — Phase 3, **здесь только пишем**.
    - **Routes** — `@Serializable data class <Name>TrainerRoute(...)` в `ui/nav/Routes.kt` + composable в `EgeApp.kt` NavHost.
    - **Точка входа** — карточка в `SubtypesScreen` сверху списка подвидов, для нужного `(subjectSlug, typeNumber)`. Иконка + tint следуют общему стилю `AppleListRow`.
    - **ViewModel** — `AndroidViewModel`, state в `MutableStateFlow<<Name>Ui>`, sealed `<TapState>` (None → FirstTap → Verdict), методы `start/check/goNext/goPrev/toggleOrder`.
    - **UI** — `LargeTitleBar` (title + «X из Y» subtitle + ⇆ chip), `AnimatedContent` со spring-slide транзишн между словами, `pointerInput` со `detectHorizontalDragGestures` (threshold 80dp), `delay(1000L) → goNext` на правильный ответ, haptic LongPress/TextHandleMove, **БЕЗ кнопки «Далее»** при неверном (только свайп).
    - Эталонные реализации: `ui/accent/` (тренажёр ударений №4, 230 слов), `ui/wordblank/` (типы 9-12, 2674 слова с пропусками).
18. **Trainer Resume pattern.** Любой тренажёр со списком слов/задач >50 элементов сохраняет прогресс в `TrainerProgressStore` (data/TrainerProgressStore.kt, DataStore Preferences под ключом `progress_<trainerId>`). На старте `vm.start()` читает `TrainerProgressStore.get(ctx, trainerId)` и кладёт в `state.pendingResume`. UI показывает `ResumeBottomSheet` (ui/common/ResumeBottomSheet.kt) с двухстадийным потоком: «Продолжить» (PrimaryButton, SystemBlue) или «Начать сначала» → второй sheet с `DangerButton` (SystemRed, новый компонент в AppleButton.kt) для подтверждения сброса. Сохранение: `persistProgress()` в каждом `goNext`/`goPrev` сохраняет `TrainerProgress(position, total, order, indices)`. Сброс: `clearProgress()` при `position+1 >= total` (последнее слово пройдено) и при `toggleOrder()` (смена режима А-Я ↔ 🎲 начинает с нуля). trainerId формат: `accent_<categoryId>` для категорий ударений, `accent_all_random`/`accent_all_alphabetical` для всех слов, `blank_<typeNumber>` для типов №9-12 русского. **savedValid** проверка: `saved.total == indices.size && saved.position in 1 until indices.size` — если данные расходятся (например, изменился словарь), sheet не показываем.
19. **Rules JSON pattern.** Справочные правила для типов задач (текст «Простейшие уравнения», «Правописание корней» и т.д.) хранятся **офлайн** в `parser/rules.json`, генерится `parser/scrapers/generate_rules.py` (вручную в Python-словаре, **никакого Claude API в runtime**). 46 правил (19 math + 27 rus) в формате `{ "math_6": { subject, type_number, title, markdown }, ... }`, по 200-500 слов Markdown каждое. Скрипт пишет обе копии: `parser/rules.json` (источник, в git) и `android/app/src/main/assets/rules.json` (копия, gitignored). Android-side: `RulesRepository.getRule(ctx, subjectSlug, typeNumber)` — кешированная загрузка из assets, маппинг `mathb` → `math_N`, `rus` → `rus_N`. UI: `RuleBottomSheet(rule, onDismiss)` с заголовком + `SimpleMarkdownRenderer` (без external deps, поддерживает `##/###` заголовки, `- */1.` списки, `**жирный**`). Кнопка «📋 Правило» в `ProblemDetailScreen` enabled только если `st.rule != null`. Обновление правил — отредактировать `MATH_RULES`/`RUS_RULES` словарь в `generate_rules.py` и заново запустить — никаких миграций.
20. **Edge swipe + horizontal problem swipe coexistence.** В Compose Navigation 2.8 нет встроенного iOS-style swipe-back. Реализован вручную в `ui/modifiers/EdgeSwipeBack.kt`: `Modifier.edgeSwipeBack` слушает pointer events на `PointerEventPass.Initial`, реагирует только если первый `down.x < edgeWidthDp` (по умолчанию 24dp), при суммарном `dragX > triggerDistanceDp` (100dp) вызывает `onSwipeBack()`. **Важно — не consume событий**: LazyColumn-прокрутка ниже работает нормально. Применяется один раз на `NavHost.modifier` в `EgeApp.kt`. **Координация с горизонтальными свайпами внутри экранов** (между задачами в `ProblemDetailScreen`, между словами в тренажёрах): свайпы внутри экрана делают свой `pointerInput` с `detectHorizontalDragGestures` и в `onDragStart` ставят `skip = startX < edgePx` (24dp) — жесты, начатые в edge-зоне, игнорируются содержимым экрана и отдаются edge swipe back. Threshold для problem swipe — 90dp (короче edge swipe в 100dp, потому что edge ловит явный жест от края). Тренажёрные свайпы (AccentTrainer/WordBlankTrainer) используют threshold 80dp без edge guard, потому что начинаются с центра.
21. **LocalDarkOverride pattern** — мгновенная смена темы без перезапуска Activity. Сама проблема: `isSystemInDarkTheme()` читает только системный флаг, и принудительная DARK/LIGHT тема в настройках приложения требует или перезапуска Activity, или хитрого CompositionLocal. Реализовано вторым способом в `ui/theme/Color.kt`: top-level `val LocalDarkOverride = staticCompositionLocalOf<Boolean?> { null }`, все динамические цвета (`Bg`/`Label`/`SystemBlue`/...) читают `LocalDarkOverride.current ?: isSystemInDarkTheme()` через приватный helper `isDark()`. В `EgeTheme(themeMode)` оборачиваем content в `CompositionLocalProvider(LocalDarkOverride provides useDark)`, где `useDark` вычисляется из `ThemeMode.AUTO/DARK/LIGHT`. `MainActivity` подписан на `AppSettingsStore.settingsFlow.collectAsState` → при смене темы в Настройках весь UI **мгновенно** перекрашивается (CompositionLocal recomposit'ит всех потребителей). HtmlRenderer тоже подписывается на `isSystemInDarkTheme()` для инверсии SVG — `isDark()` getter работает прозрачно и там, поскольку CompositionLocal активен на всём composition tree под MaterialTheme.
22. **BackupSnapshot pattern** — единая точка сборки/восстановления всех пользовательских данных в JSON. Архитектура: каждый Store (UserProfileStore, AppSettingsStore, TrainerProgressStore, FavoritesStore, AccentErrorsStore, WordBlankErrorsStore) обязан иметь триаду методов: `snapshot(context): T` (для экспорта), `restore(context, t: T)` (для импорта, **полная замена** текущих данных), `clearAll(context)` (для сброса прогресса). Для Store'ов с динамическими ключами (TrainerProgressStore с `progress_<trainerId>`, WordBlankErrorsStore с `wrong_words_t<N>`) добавляется `getAll(context)` который читает все ключи с нужным префиксом и парсит. `BackupRepository` (data/BackupRepository.kt) собирает `BackupSnapshot(version, exportedAt ISO Instant, appVersion, ...)` через `kotlinx.serialization`, `applyBackup()` зовёт все `restore()`-методы по очереди. **resetProgress() трогает только Trainer/Favorites/Errors — НЕ затрагивает Profile/Settings**, это сознательное решение: «Сбросить прогресс» в UI не должно случайно удалить имя/тему. При добавлении нового Store в Phase 3+ — расширить `BackupSnapshot` + сериализатор + соответствующие restore/clearAll вызовы.
23. **FileProvider + Intent.ACTION_SEND** для экспорта пользовательских данных. Прямой `Uri.fromFile(File)` в Android 7+ бросает `FileUriExposedException` — обязательно через `FileProvider.getUriForFile()`. Конфигурация: `<provider android:authorities="${applicationId}.fileprovider">` в `AndroidManifest.xml` + `res/xml/file_paths.xml` с `<cache-path name="backup_cache" path="."/>` (имена в paths.xml произвольные — это только namespace внутри URI, важна структура domain/path). Реализация в `data/BackupShare.kt`: пишем JSON в `context.cacheDir/ege100_backup_YYYY-MM-DD.json`, получаем `FileProvider.getUriForFile(context, "${packageName}.fileprovider", file)`, кладём в `Intent(ACTION_SEND).setType("application/json").putExtra(EXTRA_STREAM, uri).addFlags(FLAG_GRANT_READ_URI_PERMISSION)`, оборачиваем `Intent.createChooser()`. Импорт — обратное направление через `ActivityResultContracts.GetContent("application/json")` + `contentResolver.openInputStream(uri)`. **Дополнительно — пассивный Auto Backup от Android**: в манифесте `allowBackup="true"`, `fullBackupContent="@xml/backup_rules"` (Android 11-), `dataExtractionRules="@xml/data_extraction_rules"` (Android 12+) — система автоматически бэкапит `sharedpref/` и `database/` в Google аккаунт пользователя. **Обязательно исключить corpus.db** (192 MB) через `<exclude domain="database" path="corpus.db"/>` — иначе Auto Backup упирается в системный лимит 25 MB и выключается целиком.
24. **StreakStore + `onProblemSolved` hook** — единый паттерн учёта активности пользователя. Логика «дни подряд с активной подготовкой» зашита в `data/StreakStore.kt` (DataStore Preferences). День считается «активным» если решено ≥ `DAILY_GOAL` (= 10) задач — это правило-сейф из CLAUDE Safety Rule #5 «50 задач в неделю» спущенное к дневному уровню. Поля state: `currentStreak`, `maxStreak`, `todaySolvedCount`, `lastActiveDate` (ISO). При вызове `onProblemSolved()`: инкремент `today_solved`; если ровно сегодня дошли до DAILY_GOAL и день ещё не был засчитан — `current = current+1` (если `lastActive == today-1`) либо `1` (иначе), `max = max(max, current)`. На старте Главного экрана зовётся `checkValidity()` — если gap > 1 день, `current = 0` (но `max` сохраняется). **Hook подключён в 3 ViewModel'и** где пользователь подтверждает ответ: `ProblemDetailViewModel.check()` (только при `isFirstCheck`, т.е. `checkResult == Idle` — повторные клики не идут в счётчик), `AccentTrainerViewModel.tapSyllable` (при переходе в Verdict, любой ответ), `WordBlankTrainerViewModel.check()` (при первом нажатии «Проверить»). **При добавлении нового тренажёра/экрана с ответами в P3-D+** — обязательно подключить `StreakStore.onProblemSolved(ctx)` + `UserStatsStore.recordAttempt(ctx, subject, typeNumber, subtypeId, isCorrect)` в `viewModelScope.launch`. Reset через `BackupRepository.resetProgress()` чистит и streak (current/max/today).
25. **ScorePredictor + FipiScoreTable** — алгоритм прогноза ЕГЭ-балла. `data/ScorePredictor.kt` + `FipiScoreTable` зашитой таблицей «первичный→тестовый» из приказа ФИПИ-2024 (math: 0..32 → 0..100, rus: 0..50 → 0..100). Если ФИПИ изменит таблицу — обновить руками. Алгоритм `predict()`: для каждого типа задач берём `accuracy` и `coverage = min(attempts/15, 1.0)`. `basePercent` = step-функция accuracy → ожидаемый процент верных ответов: `≥0.7 → 0.85, ≥0.5 → 0.55, ≥0.3 → 0.30, иначе 0.10, при attempts==0 → 0.30 pessimistic`. Затем blend: `mixed = basePercent * coverage + 0.30 * (1 - coverage)` — при низком coverage прогноз сдвигается в pessimistic 30%, чтобы 1 решённая задача не давала «100% prediction». Вес типа = максимальный первичный балл (math: №1-12=1, №13-15=2, №16-17=3, №18-19=4; rus: №27=24, остальные=1). `expectedRaw = Σ (weight × mixed)` → `FipiScoreTable.rawToTest()` → тестовый балл. `confidence = avg(coverage)` — если <0.3 в PredictorCard показываем подсказку «Реши больше задач для точного прогноза». Цвет в UI: `testScore ≥ target → green, ≥ target-10 → orange, иначе red`.
26. **Radar 4 styles через AppSettingsStore.radarStyle** — рендерер для 4 визуализаций слабых мест. Все живут в `ui/home/RadarCard.kt`, переключаются `when (style)` от `AppSettings.radarStyle`. Severity (data/SubtypeStatsRepository.kt): `GRAY` при `attempts < 15` (порог из Safety Rule #1, чтобы серая ≠ «знаю» и закрывала смещённую выборку); далее по accuracy: RED <60%, YELLOW <80%, GREEN ≥80%. **LIST** — топ-15 не-серых отсортированных RED→YELLOW→GREEN, каждый ряд = цветная точка + название + accuracy + AppleProgressBar 4dp. **DONUT** — Canvas `drawArc(stroke=28dp, cap=Round)` для топ-10 секторов + центр «N слабых» + легенда top-5. **HEATMAP** — сетка 7 столбцов × N строк, плитки 38dp с фоном severity, текст = номер типа. **RADAR_CHART** — Canvas с 5-уровневой концентрической сеткой polygon, дата-path filled + stroke SystemBlue, агрегация 50+ подвидов в **7 крупных тем** через `aggregateThemes()` (math: Алгебра/Геометрия/Вероятность/Параметр и числа; rus: Орфография/Пунктуация/Текст и лексика). Аккуратность темы = weighted by attempts. Смена `radarStyle` в Настройках → мгновенный rerender через Convention #21 (LocalDarkOverride/composable getters) + `collectAsState(AppSettingsStore.settingsFlow)` в HomeViewModel.
27. **BackupSnapshot версионирование + backward-compat** — при добавлении новых Store'ов в бэкап **бампим minor-версию** (1.0 → 1.1 → 1.2), но старые бэкапы должны парситься. Реализация: `BackupSnapshot.CURRENT_VERSION = "1.2"`, `SUPPORTED_VERSIONS = setOf("1.0", "1.1", "1.2")`, проверка через `BackupSnapshot.isSupported(version)`. Новые поля (P3-B — `userStats`/`streak`; P3-C — `errorLog`/`attemptLog`) обязательно с **default-значениями** (для коллекций — `emptyList()`). `Json { ignoreUnknownKeys = true }` уже выставлен в BackupRepository — позволяет читать backup'ы с лишними полями (forward-compat для старых версий приложения, в которых поле было удалено). При удалении Store-поля — НЕ удалять из BackupSnapshot, оставлять для совместимости (помечать `@Deprecated`). При **изменении формата** существующего поля (например, переход с `String` дат на `Long` epoch) — нужен migration-code в `applyBackup()` под старую версию + бамп major-версии.
28. **UserDataDatabase отдельная от corpus.db** — Room v1 в `user_data.db`, не часть `corpus.db`. Причина: corpus.db поставляется с APK (192 MB read-only, `Room.createFromAsset`), а журнал ошибок и попыток должен расти и меняться — нужна writable БД. Структура: `data/UserDataDatabase.kt` со списком entities `[ErrorLogEntity, AttemptLogEntity]`, `exportSchema = true` (JSON-схема в `android/app/schemas/com.daniel.ege100.data.UserDataDatabase/1.json` в git). Сборка через `Room.databaseBuilder(ctx, UserDataDatabase::class.java, "user_data.db").build()` + singleton companion. **Foreign keys к corpus.db невозможны** (cross-database joins в Room не поддерживаются) — хранится только `problem_id: Long`, целостность валидируется на уровне приложения (ErrorsListViewModel.refresh подтягивает каждый problem через CatalogDao.getProblem; orphan'ы при миграции парсера → `row.problem == null` → UI показывает «задача недоступна»). При добавлении новых таблиц в P3-D+ (MockExamScheduleEntity, AiConversationEntity и т.п.) — версия 2 + Migration через `addMigrations()` на builder. Сами entities помечать `@Entity` с обязательными `@Index` на колонки используемые в WHERE/ORDER BY (timestamp, problem_id, subject).
29. **AttemptLog + ErrorLog hook паттерн** — расширение Convention #24 (StreakStore.onProblemSolved). В тех же 3 ViewModel'ях (ProblemDetail / AccentTrainer / WordBlank) рядом с `UserStatsStore.recordAttempt` + `StreakStore.onProblemSolved` пишется `UserDataDatabase.get(ctx).attemptLogDao().insert(AttemptLogEntity(...))`. **ErrorLog только в ProblemDetail** — при `!isCorrect && problem.answer != null`. Тренажёры (Accent/WordBlank) НЕ пишут в error_log: у них уже есть свои `AccentErrorsStore`/`WordBlankErrorsStore` со словами (для будущего UI «только ошибки»). Поля `source` в attempt_log: `"problem"`, `"errors_retry"` (когда задача открыта из «Перерешать»), `"accent_trainer"`, `"wordblank_trainer"`, `"quick_trainer"`. Поле `durationMs` пишется только из ProblemDetail (там есть `openedAtMs` в state); тренажёры пишут 0. При **«Перерешать» + правильный ответ** — `errorLogDao.markAllResolvedFor(problem.id)` помечает все непререшённые записи этой задачи одной операцией.
30. **CsvExporter pattern + UTF-8 BOM** — экспорт `attempt_log` в CSV для Excel/Google Sheets через тот же FileProvider механизм что и JSON-бэкап (Convention #23), но с MIME `text/csv`. **Критично — UTF-8 BOM** (`﻿` U+FEFF) первым символом файла: без BOM Excel читает кириллицу как кракозябры, Google Sheets читает корректно в обоих случаях — универсальный вариант с BOM. Формат: 9 колонок `timestamp_ms,date,subject,type_number,subtype_id,problem_id,is_correct,duration_ms,source`. Дата экранирована в кавычки (содержит пробел), остальные поля без кавычек (нет запятых в значениях). Расширение `.csv`, имя `ege100_attempts_<YYYY-MM-DD>.csv` в `cacheDir/`. Использовать `Intent.createChooser(Intent(ACTION_SEND).setType("text/csv"))` — share-sheet даёт пользователю выбор Telegram/Drive/Files. **При добавлении новых типов CSV-экспортов в P3-D+** (mock exam history, weekly summary) — следовать тому же паттерну с BOM и тем же FileProvider authority. Не пытаться открывать CSV внутри приложения через `Intent.ACTION_VIEW` — Android не имеет встроенного CSV-вьюера, пользователь должен сохранить в Excel/Sheets.
31. **AnswerBlock с `revealed` флагом** — правильные ответы в журнале ошибок скрыты по умолчанию. Любой UI-компонент, который показывает «твой ответ vs правильный ответ» **должен скрывать правильный ответ под тап** — иначе пользователь видит ответ ещё до того как нажал «Перерешать», и кнопка теряет смысл. Реализация в `ErrorsListScreen.AnswerBlock(userAnswer, correctAnswer, revealed, onReveal)`: всегда видна «Твой: X» красным, под ней либо ссылка-кнопка «Показать правильный ответ» (SystemBlue 13sp Medium), либо «Верно: Y» зелёным. В `ErrorCard`: `var revealCorrect by remember(row.error.id) { mutableStateOf(row.error.isResolved) }` — для уже перерешённых задач (`isResolved=true`) ответ виден сразу, пользователь его и так знает. **Ключ `row.error.id` в `remember`** обязателен — иначе при scroll LazyColumn'а recycling переиспользует state между разными карточками. **При добавлении новых UI-местах с спойлерами** (например, AI-ответ в Phase 4) — следовать тому же паттерну: state хранится локально в карточке, ключ = id записи, можно прокидывать через ViewModel если нужна персистентность раскрытия между сессиями.
32. **NotEnoughDataHint pattern** — единый fallback для радара (и других мульти-стиль виджетов) когда данных недостаточно. Проблема: у радара 4 стиля (LIST/DONUT/HEATMAP/RADAR_CHART), и каждый раньше писал свой EmptyHint с разным текстом — пользователь переключал стиль и думал что **смена не сработала**, хотя на деле радар каждый раз ругался на нехватку данных. Решение в `ui/home/RadarCard.kt`: проверка `coloredCount = stats.count { it.severity != Severity.GRAY }` поднята **на уровень RadarCard** (до switch по style). Если `coloredCount == 0` — показываем единый `NotEnoughDataHint(stats)` вместо переключаемых стилей. NotEnoughDataHint содержит: (1) основной текст с порогом «Радар появится, когда хотя бы в одном подвиде накопится 15+ попыток», (2) live-прогресс «Сейчас: N попыток в M подвидах. Максимум в одном: K / 15» с правильным склонением подвиде/подвидах. Гарантия для каждого RadarXxx-композебла: туда мы попадаем только при `coloredCount > 0`, что упрощает код (можно убрать локальные fallback'и). **Применять паттерн к любой мульти-стиль визуализации** — общая проверка на «есть ли что показать» наверху, единый fallback с прогрессом до порога, чистые render-функции для каждого стиля без дублирующихся `if (empty)` проверок.
33. **MockExamSchedule pattern** — расписание пробников от install_date. `data/MockExamSchedule.kt` хранит **только install_date** в DataStore (одно поле). При `ensureInstallDate()` первый вызов сохраняет сегодняшнюю дату навсегда — это техническая дата для отсчёта (Safety Rule #3). Расписание считается на лету: первый пробник = `max(install + 28 дней, today + 14 дней)` (гарантирует минимум 2 недели до первого даже если приложение установили незадолго до ЕГЭ), дальше каждые 21 день, последний ≤ `examDate - 7`, максимум 16 точек. Статусы UPCOMING/TODAY/PAST вычисляются от текущей даты. **install_date** — священная: `BackupRepository.resetProgress()` его **НЕ трогает**, иначе расписание сдвинется и пользователь потеряет привязку к календарю. При импорте бэкапа — `restoreInstallDate()` перезаписывает (это часть бэкапа). При добавлении новых stage'ей с расписаниями (мини-тесты, weekly review) — следовать тому же паттерну: одна техническая дата + детерминированный расчёт от неё.
34. **WorkManager periodic workers + AppSettings gating** — push-уведомления через 3 ежедневных воркера: StreakReminder 20:00, DailyReminder 9:00, MockExamReminder 9:00. Регистрация в `EgeApplication.onCreate()` через `WorkManager.enqueueUniquePeriodicWork(name, ExistingPeriodicWorkPolicy.KEEP, ...)` — `KEEP` не пересоздаёт уже запланированные jobs при перезапуске. Initial delay считается через `delayUntil(hour, minute)` — миллисекунды до следующего наступления локального времени. **Гейт через AppSettings**: каждый `doWork()` в первой строке читает соответствующий `notifyXxx` флаг из AppSettingsStore — если выключено, возвращает `Result.success()` без действий. Это даёт пользователю чистый ON/OFF через Switch в Настройках без отмены WorkManager-расписания. **POST_NOTIFICATIONS permission** (Android 13+): в манифесте `<uses-permission>` + `ActivityResultContracts.RequestPermission()` в MainActivity при старте. `NotificationHelper.show()` сам проверяет permission и тихо игнорирует если нет — это ожидаемое поведение (показываем что можем). **NotificationChannel** создаётся идемпотентно (`getNotificationChannel(id)` → null check) — повторные вызовы no-op. WorkManager не гарантирует точное время (Android DOZE может задержать до часа) — для напоминаний приемлемо.
35. **SafetyGuards from premortem (#5 50/week + #6 300/8weeks)** — визуальные индикаторы что подготовка замедляется. **Это политические инструменты, не блокировки**: пользователь видит карточку/диалог, но никаких реальных ограничений нет (Safety Rule #5 «код заморожен» — это инструкция Claude Code в CLAUDE.md, не фича приложения). `data/SafetyGuardsStore.kt` хранит флаги `weeklyGuardActive`/`eightWeekGuardActive` + кэш счётчика + `lastCheckedEightWeekPeriod`. `SafetyGuardsChecker.checkWeekly()` запускается в HomeViewModel.init: считает attempts за прошлую календарную неделю (понедельник-воскресенье); при <50 активирует `WeeklyGuardCard` под шапкой Главного (SystemRedTint). `checkEightWeek()` каждые 8 недель от install_date: считает прошлый 8-недельный период; при <300 показывает `EightWeekCheckpointDialog` (AlertDialog) **один раз** для текущего периода (защита через `lastCheckedEightWeekPeriod >= currentPeriod`). После confirm — `dismissEightWeek` снимает флаг. AttemptLogDao.getCountBetween(startMs, endMs) — общий query для обеих проверок.
36. **BackupSnapshot v1.3** — третий бамп версии с расширением. Добавлены `mockExamResults: List<MockExamResultRecord> = emptyList()`, `installDate: String? = null`, `guardsState: GuardsState = GuardsState()`. `SUPPORTED_VERSIONS = setOf("1.0", "1.1", "1.2", "1.3")`. Convention #27 продолжает работать: default-значения новых полей + `ignoreUnknownKeys = true` обеспечивают backward-compat. `applyBackup`: `mockDao.deleteAll()` перед insert (autoGenerate id), `MockExamSchedule.restoreInstallDate(parsed)` если installDate в бэкапе, `SafetyGuardsStore.restore(guardsState)`. **resetProgress сохраняет install_date** (Convention #33), но чистит mock_exam_results + safety_guards + всё что было до. При следующем бампе версии (v1.4) — те же правила: default-значения + добавить в SUPPORTED_VERSIONS, не удалять поля.
37. **typesCovered критерий — 15+ attempts И accuracy ≥ 70%** — «освоенный» тип задач определяется в `StatsViewModel.refresh`: `(mathStats + rusStats).count { it.attempts >= 15 && it.accuracy >= 0.70f }`. Раньше было `count { it.attempts > 0 }` — это давало ложную мотивацию («Освоено: 1 из 46» после одной решённой задачи). Пороги выбраны так чтобы соответствовать Safety Rule #1 (15 attempts для окрашивания радар-сектора) + accuracy ≥ 70% как уровень «реально знает тему». **UI расширения:** `AchievementRow` принимает `hint: String? = null` и `accent: Boolean = true` — для «Освоено типов» hint = «15+ решений · точность 70%+» под title, `accent = (count > 0)` → серый цвет если 0 (LabelSecondary вместо SystemBlue). В `TypeAccuracyRow` — ✓ SystemGreen рядом с названием типа если `mastered = attempts >= 15 && accuracy >= 0.70f`. При добавлении других «mastery»-метрик в Phase 4+ (например, «освоено правил») — следовать тому же паттерну: жёсткий порог покрытия + порог accuracy + явная подсказка в UI.
38. **AiProvider interface + AiProviderRegistry** — три провайдера AI через единый интерфейс. `ai/AiProvider.kt` определяет `sealed interface AiResponse { Success(text, tokensUsed) / Error(message, isAuthError, isRateLimit) }`, `enum AiProviderType { OPENROUTER, GEMINI, ANTHROPIC }`, `data class AiModel(id, displayName, description, costHint, isFree)`, и `interface AiProvider` с полями `type/displayName/description/signupUrl/keyHint/availableModels/defaultModelId` + `suspend fun ask(question, context, modelId, apiKey): AiResponse`. **Системный промпт** `EGE100_SYSTEM_PROMPT` общий, явно говорит модели что «Задача:» — реальный текст с экрана и формулы расшифрованы alt-текстом в скобках. `ai/Providers.kt` — три реализации через OkHttp + org.json (без kotlinx.serialization для AI — лишний оверкилл, парсим вручную). OpenRouterProvider: POST `openrouter.ai/api/v1/chat/completions` с OpenAI-совместимым форматом + headers `HTTP-Referer` + `X-Title`. GeminiProvider: POST `generativelanguage.googleapis.com/v1beta/models/$modelId:generateContent?key=$apiKey` с `contents[parts[text]]` форматом. AnthropicProvider: POST `api.anthropic.com/v1/messages` с `x-api-key` + `anthropic-version: 2023-06-01`. **Общий error handling** через приватный `runRequest` helper: 401/403→`isAuthError`, 404→«Эта модель сейчас недоступна» (Convention #46), 429→`isRateLimit`, network exception→generic. **AiProviderRegistry** — singleton с lazy `OkHttpClient` (15s connect, 60s read — AI ответ может быть медленным), все три провайдера регистрируются и достаются по `AiProviderType`. **При добавлении нового провайдера в P5+** — наследовать AiProvider, прописать список моделей, добавить в Registry, не трогать остальное.
39. **SecureKeyStore через EncryptedSharedPreferences** — API ключи AI хранятся в **отдельном** файле `ai_secure_keys` через `androidx.security:security-crypto`. Tink-backed AES-256-SIV для key обфускации + AES-256-GCM для value шифрования. Ключи привязаны к Android Keystore текущего устройства — при переустановке приложения теряются. `data/SecureKeyStore.kt` API: `saveKey(provider, key)` / `getKey(provider): String?` / `hasKey(provider): Boolean` / `removeKey(provider)` / `clearAll()`. Параметр `provider` — строка `AiProviderType.name` (`"OPENROUTER"`/`"GEMINI"`/`"ANTHROPIC"`). **Никогда не хранить ключи в обычном DataStore** — это plaintext в Preferences.xml, видно через `adb run-as`. **Никогда не логировать ключи** даже в Log.d. **Никогда не включать в backup** — см. Convention #40.
40. **API-ключи исключены из всех видов бэкапа** — `ai_secure_keys.xml` тренировано исключается из:
    - **Backup JSON-экспорта** (`BackupRepository.kt`): `aiSettings` сериализуется, но `secureKeys` — нет. Импорт никогда не пишет ключи; пользователь вводит заново.
    - **Auto Backup Android 11-** (`res/xml/backup_rules.xml`): `<exclude domain="sharedpref" path="ai_secure_keys.xml" />`.
    - **D2D Migration Android 12+** (`res/xml/data_extraction_rules.xml`): тот же `<exclude>` в обоих `<cloud-backup>` и `<device-transfer>`.
    Причина: ключи Encrypted SharedPreferences привязаны к Keystore исходного устройства. На новом устройстве ключ из бэкапа расшифруется как мусор — лучше пусть пользователь введёт заново. **При добавлении новых secret-prefs файлов в P5+** (например, `app_passcode.xml` или OAuth tokens) — тот же паттерн: исключить из 3 мест.
41. **AiSettingsStore + AiResponseCache + daily limit** — отдельный DataStore `ai_settings` (не пересекается с `app_settings`). Поля: `activeProvider: AiProviderType`, `modelByProvider: Map<String, String>`, `dailyLimit: Int = 50`, `todayUsage: Int = 0`, `todayDate: String`. **Auto-reset** дневного счётчика: `settingsFlow` сравнивает сохранённый `today_date` с `LocalDate.now()`, если не совпадает — возвращает `todayUsage = 0`; старая дата перезаписывается при первом `incrementTodayUsage` в новый день. **AiResponseCache** — таблица `ai_response_cache` в `user_data.db` (Room v3→v4 миграция), Entity `AiResponseCacheEntity(cacheKey PK, response, cachedAt indexed)`, Dao с `get(cacheKey)`/`put(entity)`/`deleteOlderThan(cutoffMs)`. **Cache key** = `sha256("$providerName|$modelId|$question|$context")` — один и тот же вопрос на одну и ту же задачу не сжигает токены дважды. **TTL 30 дней** — в `EgeApplication.onCreate` запускается `cleanupOldAiCache()` через `GlobalScope.launch(Dispatchers.IO)` (warning о delicate API приемлем — это однократный fire-and-forget при старте). Cache hit показывает badge «из кеша» в UI и **не** инкрементирует `todayUsage`. Daily limit check: `canMakeRequest()` сравнивает `todayUsage < dailyLimit` ДО `provider.ask()`, при перебое — error message «Достигнут дневной лимит (N). Завтра сбросится автоматически».
42. **MockExamRunner — раздельные composeMath / composeRus** — пробник идёт по одному предмету, не вперемешку. `composeMockMath()` — 19 задач, по одной случайной из каждого типа №1..№19 (math профильной). `composeMockRus()` — 26 задач, по одной случайной из каждого типа №1..№26 (русский, **без сочинения** №27 — оно требует судьи). Подбор через `CatalogDao.getProblemIdsByTypeNumber(subjectSlug, typeNumber)` + `random()`. `SubjectChooserBottomSheet` показывается на «Старт пробника» — пользователь выбирает Математика 19 или Русский 26. `MockExamRunnerRoute(planIndex, subject = "math", fipiVariantId = null)` — `subject` определяет какой composer вызовется. Старый смешанный `composeMix()` удалён (8 math + 8 rus подряд было нерепрезентативно — реальный ЕГЭ идёт по одному предмету за день).
43. **FIPI variants pre-bundled через `parse_fipi_variants.py`** — 6 «эталонных» вариантов КИМ ФИПИ (3 math по 19 задач + 3 rus по 26 задач) собираются офлайн из corpus.db со **строгим seed на вариант** (id вариант → `Random(seed)` → стабильный набор задач), пишутся в `parser/fipi_variants.json` (в git) + копия `android/app/src/main/assets/fipi_variants.json` (gitignored — runtime копия). `FipiVariantsRepository.load(context)` кеширует in-memory. UI: `FipiVariantsScreen` (доступ через QuickActionsCard на Главном + entry в `MockExamCalendarScreen`) — список вариантов сгруппирован по предмету, для пройденных показывается результат. Пройденный FIPI-вариант сохраняется в `mock_exam_results` с `planIndex = -1` + `source = "fipi"` + `scheduledDate = fipiVariantId` (для группировки в FipiVariantsScreen). При парсинге Python-скрипта **не печатать Cyrillic в stdout** на Windows cp1251-terminal (UnicodeEncodeError) — выводить только id и count.
44. **mock_exam_results.source + TrendChart** — каждая запись пробника помечается `source: String` (default `"internal"`, FIPI = `"fipi"`). `MockExamHistoryScreen` показывает все попытки sorted by completedDate DESC + `TrendCard` со встроенным Canvas-chart'ом: math синим (SystemBlue), rus оранжевым (SystemOrange), целевой балл — пунктирная зелёная линия (SystemGreen, dashed PathEffect). По X — completedDate (равноотстоящие точки, не пропорционально времени — иначе ранние результаты прижмутся к левому краю). Точка чертится `drawCircle(radius = 6dp)`, линии между точками — `drawLine(strokeWidth = 2dp, cap = Round)`. Если в истории <2 точек для предмета — линия не рисуется (только точка). При добавлении новых типов истории (weekly summary, daily quiz) — тот же паттерн: общая Entity с `source` discriminator + отдельный экран истории.
45. **INTERNET + ACCESS_NETWORK_STATE permissions в AndroidManifest для AI** — до Phase 4 приложение было **полностью офлайн** (corpus.db + assets + DataStore), поэтому INTERNET permission никогда не объявлялся. Когда добавили AI-запросы к OpenRouter/Gemini/Anthropic, при первой попытке OkHttp бросает `SecurityException: Permission denied` (виден как «Ошибка сети: Permission denied» в UI). Исправление: в `AndroidManifest.xml` сразу после `<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />` добавить **обе** строки: `<uses-permission android:name="android.permission.INTERNET" />` и `<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />`. Оба permission'а — `normal` protection level, грантятся автоматически без запроса у пользователя. INTERNET — обязателен для OkHttp; ACCESS_NETWORK_STATE — желателен для проверки сетевого состояния (фактически no-op без NetworkSecurityConfig overrides, но лучше иметь). **При добавлении новых сетевых фич в P5+** (синхронизация, шаринг, OAuth) — проверить что permission уже есть, не дублировать.
46. **OpenRouter free models rotation — `openrouter/free` как default** — OpenRouter ротирует каталог бесплатных моделей примерно раз в 1-3 месяца. Конкретный id (например `deepseek/deepseek-r1:free`) **протухает** — провайдер возвращает 404 `No endpoints found`. Решение: использовать `openrouter/free` как auto-router — OpenRouter сам выбирает доступную бесплатную модель из текущего каталога. Прописан как **первый** в `OpenRouterProvider.availableModels` и `defaultModelId = "openrouter/free"` + дефолт в `AiSettingsStore` для OPENROUTER. Остальные конкретные free-модели (DeepSeek V4 Flash, DeepSeek R1, Llama 3.3 70B, Qwen 2.5 72B, Gemini 2.0 Flash) оставлены в списке как **fallback** на случай если auto-router не подойдёт пользователю. Обработка 404 в `runRequest` (Convention #38): `AiResponse.Error("Эта модель сейчас недоступна. Попробуй другую в Настройках → Модель.")` — пользователь меняет модель в UI без перезапуска. **При обнаружении что и `openrouter/free` 404'нул в будущем** — это сигнал что OpenRouter переименовали auto-router; проверить актуальный id на openrouter.ai/docs/models и обновить три места (`OpenRouterProvider.defaultModelId`, `availableModels[0].id`, `AiSettingsStore.defaultModels()`).
47. **AI context fix — Jsoup парсинг HTML + защита от пустого + диагностика** — старый `stripHtmlForAi` резал теги regex'ом `<[^>]+>`, что **уничтожало `<img alt="...">`** — у sdamgia формулы хранятся как `<img class="tex" alt="дробь: числитель: ..., знаменатель: ...">` и без alt модель видела условие без формул, отвечала наугад. Новая версия в `ProblemDetailScreen.stripHtmlForAi`: `Jsoup.parse(html).select("img[alt]").forEach { img -> img.replaceWith(TextNode(" $alt ")) }` — alt-текст вшивается inline. Лимит 2000 → **4000 символов** (Sonnet/Opus легко съедают). Обрезка по последнему `.!?` если попал в зону >3000 → не рвём предложение; fallback — по последнему пробелу. Защита `if (html.isBlank()) return ""`. **В `AskAiViewModel.ask` до проверки apiKey**: `if (context.isBlank()) → AiResponse.Error("Не удалось извлечь условие задачи. Скриншот сделай отдельно.")` — иначе пустой context сжигал токены и провоцировал угадывание. **Диагностика через `android.util.Log.d("AskAi", ...)`**: Question / Context length+preview(200) / Provider+model — для `adb logcat -s AskAi:D`. **Системный промпт `EGE100_SYSTEM_PROMPT`** усилен: явное указание что «Задача:» — фактический текст с экрана, формулы расшифрованы alt-текстом в скобках, допустим лёгкий markdown (UI рендерит SimpleMarkdownRenderer), максимум 350 слов. **При добавлении других AI-фич в P5+** (генератор похожих задач, оценка сочинения) — тот же паттерн: Jsoup для HTML → текст, защита от пустого, лимит по символам с обрезкой по концу предложения.
48. **AnswerChecker — единая проверка ответов с поддержкой множественных вариантов** — `data/AnswerChecker.kt` решает критический баг старого кода в `ProblemDetailScreen.matchesAnswer` и `MockExamRunnerScreen.matches`: они для `answer_format = "alternatives"` дробили ответ по **пробелу**, а sdamgia использует разделитель **`|`** (`"красивая|красива"`, `"235|253|325|352|523|532"`) → пользователь не мог ввести ни один вариант. **`isCorrect(userAnswer, correctAnswer, answerFormat)`** делает: (1) `normalize` = trim + lowercase + `,→.` + collapse whitespace + `trimEnd('.')` (для multipart `"12345678."`); (2) `splitVariants` дробит по `|` и `;` (defensively на случай если sdamgia добавит); (3) прямое сравнение с любым нормализованным вариантом; (4) **`multipart` fallback**: токены без учёта порядка (`"16 27"` ↔ `"27 16"`); (5) **цифровой fallback**: если ответ полностью цифры — допускаем перестановку (`"14"` ↔ `"41"`). **`hasMultipleVariants(answer, format)`** — для UI-подсказки «💡 Можно ввести любой из правильных вариантов» под полем ввода (показывается когда есть `|`/`;` или `format ∈ {alternatives, multipart}`). Подключено в 3 местах: `ProblemDetailViewModel.check`, `MockExamRunnerViewModel.check`, `WordBlankTrainerViewModel.check`. AccentTrainer не использует (тап-based, не текст-ввод). **При добавлении новых форматов sdamgia в Phase 5+** — расширять `splitVariants`/`isCorrect`, а не плодить параллельные `matches*` функции.
49. **LatexCleaner + усиленный EGE100_SYSTEM_PROMPT — двойная защита от LaTeX** — модели иногда присылают `\(\frac{4}{7}\)`, `\cdot`, `\sqrt{x}`, `\alpha`, которые `SimpleMarkdownRenderer` не умеет рендерить → пользователь видит «техно-кашу» из обратных слешей и фигурных скобок. **Защита (1) — системный промпт** в `ai/AiProvider.kt`: явное «КРИТИЧНО — НИКОГДА не используй LaTeX. Это твоё САМОЕ ВАЖНОЕ правило. Приложение НЕ рендерит \ ( ) \ [ ] \frac \sqrt». Список из 6 правил «как надо» («дроби через /», «корни через √», «греческие юникод-символами»), запрещённые символы `\ $ { } ^{ _{`, пример решения «как ИИ должен ответить». 350 слов лимит. Так как это не `const val` теперь (нельзя trimIndent в compile-time константе) — обычный top-level `val`. **Защита (2) — fallback-чистка** в `ai/LatexCleaner.kt`. `LatexCleaner.clean(text)` чистит то что модель всё-таки прислала: `\(...\)`/`\[...\]`/`$...$` → текст; `\frac{a}{b}` → `a/b` (3 прохода для вложенных дробей) с упрощением одиночных скобок `(4)/(7)` → `4/7`; `\sqrt{x}` → `√x`; `x^{2}`/`_{i}` → `x^2`/`_i`; 36 греческих букв (alpha..omega + Alpha..Omega + varepsilon/varphi/vartheta) → α..ω/Α..Ω; 21 матсимвол (`\leq`/`\le` → ≤, `\cdot` → ·, `\to` → →, `\infty` → ∞, `\ldots` → …, `\in`/`\notin`/`\subset`/`\cup`/`\cap`/`\forall`/`\exists`...). Остаточные `\command` и `{}` удаляются. Тройные newlines → двойные. Применяется **ОДИН РАЗ** в `AskAiViewModel.ask()` **перед записью в кеш** — значит повторный запрос на тот же вопрос отдаёт уже-очищенный текст без чистки и без токенов. **При добавлении новых LaTeX-конструкций в P5+** (matrices, integrals, sum/prod) — расширять `LatexCleaner.COMPARE` map; не пытаться рендерить LaTeX визуально (это требует Math Jax в WebView — отказались в Stage 0).
50. **Размер формул в HtmlRenderer — minHeight + scaleFactor** — после Phase 4 пользователь жаловался что Math №6 «4/7 · x = 7 3/7» отображается крошечной по сравнению с заголовком «Найдите корень уравнения:». Старый код брал `naturalHeight` SVG из viewBox (24-36dp для простых формул) — мелко. Новые правила: **inline формулы**: `scaleFactor` 1.4 → 1.6, минимум 20sp → **28sp**. На base=18sp inline-формула теперь 28sp (раньше 25). **Block-формулы** (`BlockFormula` для крупных `<img class="tex">` после classifier'а isLargeFormula): `effectiveHeight = clamp(naturalHeight × 1.4, 48dp, 120dp)`. Минимум 48dp — простые block-формулы вроде «4/7 · x = 7 3/7» теперь читаемы. Максимум 120dp оставлен (cap для огромных формул). **Illustrations** (`BlockIllustration` для не-tex иллюстраций — чертежи, графики): добавлен `minHeight = 120.dp` к `heightIn(min, max)`. Маленькие чертежи больше не выглядят обрезком; `fillMaxWidth()` уже был. **При жалобах в P5+ на «всё ещё мелко»** — крутить inline 1.6→1.8 и block min 48→56; не трогать `formulaMaxDp = 120` (там cap для случая когда SVG огромен по viewBox).
51. **NavHost slide-spring 0.85f — iOS-look** — был `dampingRatio = Spring.DampingRatioNoBouncy` (= 1.0) + StiffnessMediumLow. Линейный slide без «жизни». Phase 4 Stage P4-C поменял на `0.85f` (лёгкая пружина) — даёт ощущение «попружинило и встало», характерное для iOS. Stiffness не трогаем (тот же MediumLow). Применяется ко всем 4 transitions (`forwardEnter`/`forwardExit`/`backEnter`/`backExit`) через единственный `NAV_SPRING`-объект. Tab-switching через `tabFadeEnter`/`tabFadeExit` (без slide) остался без изменений. **При жалобе на «слишком пружинит»** — крутить 0.85→0.9 (меньше bounce). При жалобе «слишком вязко» — 0.85→0.75 (больше).
52. **«Проверить» → «Далее →» при verdict=CORRECT — экономия движения** — в `ProblemDetailScreen` основная `PrimaryButton` теперь меняет text+onClick в зависимости от `CheckResult`: (1) `Correct` + `hasNext` → «Далее →» → `vm.goNext()`; (2) `Correct` + `!hasNext` → «Готово» → `vm.goNext()` (на крайней задаче это no-op, но без отдельной ветки); (3) `userAnswer.isBlank()` → «Показать решение» → `vm.check()` (раскрывает решение); (4) иначе → «Проверить» → `vm.check()`. Палец **не двигается** — тап в той же позиции переключается между «Проверить» и «Далее →». При `Wrong` остаётся «Проверить» (повторный клик допустим, в `vm.check` есть гард `isFirstCheck` — статистика не дублируется). MockExamRunner был обновлён в Phase 4 Stage A+B со своей логикой (Correct/Wrong → отдельный VerdictBanner + «Далее →»). **При добавлении новых сценариев** (например «Подсказка» как 3-й state в P5) — расширять `when` в той же кнопке, не дублировать.
53. **AI в тренажёрах + кастомные quick questions** — `AskAiBottomSheet` получил параметр `customQuickQuestions: List<QuickQuestion>? = null`. `data class QuickQuestion(label, question)` — label идёт в чип, question — в `vm.ask()`. Если null — дефолтные 4 чипа задач («Объясни решение», «Где ошибка?» с substitution userAnswer, «Какая формула?», «Похожий пример»). Тренажёры передают **свои** 4 чипа: «Почему этот слог/буква?», «Какое правило?», «Похожие слова», «Запомнить» (мнемоника). Каждый чип содержит embedded `${word.word}` для AI-контекста. **Контекст** для AccentTrainer: «Слово: $word. Правильное ударение на слог «$correctSyllable» ($highlighted). Пользователь ошибочно поставил ударение на слог «$selectedSyllable»». Для WordBlankTrainer: «Слово с пропуском: ${masked.replace('..','_')}. Полное слово: $full. Правильная буква: «$correctAnswer». Пользователь ввёл: «$userAnswer»». **Кнопка «🤖 Спросить ИИ»** (SecondaryButton, не accent) появляется в trainer body **после Verdict** (для accent — `tap is SyllableTapState.Verdict`; для wordblank — `state is BlankInputState.Verdict`). Перед Verdict — кнопка скрыта (Safety Rule #2 «Сначала попробуй» автоматически выполнен — Verdict уже зафиксировал ответ). `onOpenAiSettings` прокинут через EgeApp в обе trainer-функции, ведёт на `SettingsRoute`. **При добавлении других тренажёров в Phase 5+** — следовать тому же паттерну: SecondaryButton с эмодзи + showAi var + AskAiBottomSheet с customQuickQuestions.
54. **trainerWordsLearned + AI error handling + Backup v1.6** — три точечные правки в одной Convention из-за общей принадлежности к финальной полировке Phase 4. **(1) trainerWordsLearned** — счётчик правильно отгаданных слов в тренажёрах ударений + пропусков. Раньше `StatsScreen.JournalViewModel` подсчитывал `wordsLearned = accentErrors + wordBlankErrors` — это **число ошибок**, что отрицательно мотивирует и часто остаётся 0 у активного пользователя. Сейчас отдельный `intPreferencesKey("trainer_words_learned")` в `UserStatsStore`, инкрементируется в `AccentTrainerViewModel.tapSyllable` и `WordBlankTrainerViewModel.check` **только при isRight=true** рядом с `recordAttempt`/`StreakStore.onProblemSolved`/`attempt_log.insert`. `StatsScreen` теперь читает `UserStatsStore.getTrainerWordsLearned(ctx)`. `AccentErrorsStore`/`WordBlankErrorsStore` импорты удалены — больше не используются в Stats. **(2) Error handling в AI-провайдерах** — `runRequest` принимает `providerLabel: String = "AI-провайдер"`. Расширенная логика: 5xx коды → `«$providerLabel временно недоступен (ошибка N). Попробуй через пару минут или переключись на другой провайдер в Настройках»` (`isRateLimit = false` — это не connectivity-проблема пользователя). JSONException в 200..299 → отдельное сообщение «Неожиданный ответ от сервера $providerLabel. Попробуй позже». 4xx прочие → код без HTML-снiппета (старая версия отдавала первые 200 символов response body — это могла быть HTML-страница ошибки от Google). Каждый из 3 провайдеров (OpenRouter/Gemini/Anthropic) передаёт свой `providerLabel`. **(3) Backup v1.6** — `CURRENT_VERSION = "1.6"`, `SUPPORTED_VERSIONS = ["1.0".."1.6"]`. Сам JSON-формат не меняется (новое поле в `userStats.trainerWordsLearned` — Int с default=0), поэтому старые бэкапы v1.0-v1.5 парсятся OK благодаря `ignoreUnknownKeys = true` + default-значениям (Convention #27). При следующем бампе (v1.7) — те же правила.
55. **Defensive try/catch + global crash handler** — в Phase 4 Stage P4-C2 пользователь сообщил о краше («приложение закрыто из-за ошибки»), stack trace не получен (нет USB-отладки). Аудит показал что hot paths не имели обёрток. Сейчас три уровня защиты: **(1) AnswerChecker.isCorrect** — сигнатура расширена до `String?` для `correctAnswer`. Вся логика обёрнута в `try { checkImpl(...) } catch (e) { Log.e + fallback на userAnswer.trim().equals(correctAnswer.trim(), ignoreCase=true) }`. **(2) LatexCleaner.clean** — try/catch вокруг regex-цепочки, fallback = вернуть исходный input (пусть пользователь увидит сырой `\frac` вместо краша). **(3) AskAiViewModel.ask()** — try/catch вокруг всей корутины, при exception state.error = «Внутренняя ошибка: ${e.message?.take(120) ?: e.javaClass.simpleName}». **(4) Global handler** — `EgeApplication.installCrashHandler()` устанавливает `Thread.setDefaultUncaughtExceptionHandler` ДО `super.onCreate()` finished, который пишет в `Log.e("EgeApp", "UNCAUGHT EXCEPTION on thread '${name}'", exception)` и передаёт дальше системному handler'у. При следующем падении и подключении adb: `adb logcat -s EgeApp AndroidRuntime:E -d -t 200`. **При добавлении новых hot paths в P5+** (генератор задач, оценка сочинения, синхронизация) — следовать паттерну: try/catch + Log.e с уникальным tag + UI-fallback state с понятным сообщением.
56. **pendingAdvanceJob — auto-advance координирует с modal sheets** — в P4-C delay 1000мс после Verdict.isRight жил в `LaunchedEffect(st.tap, st.position)` Screen'а. Если пользователь успевал тапнуть «🤖 ИИ» — окно открывалось, но через 1 сек экран всё равно переключал слово, контекст AI становился «не про то слово». Сейчас Job на ViewModel: `private var pendingAdvanceJob: Job? = null`, `scheduleAutoAdvance(delayMs)` отменяет старый и запускает новый, `onAskAiOpened()` отменяет, `onAskAiClosed(wasRight)` запускает через 500мс если wasRight. Триггер `scheduleAutoAdvance(1000L)` — внутри `tapSyllable`/`check` ViewModel'я (где `Verdict` сетится), не в Screen — VM сам управляет таймером. В Screen остался только `LaunchedEffect(showAi) { if (showAi) vm.onAskAiOpened() }` + `onDismiss = { ... ; vm.onAskAiClosed(verdict.isRight) }`. **При добавлении новых modal'ов в P5+** (например AI-чат, словарь правил) — использовать тот же паттерн pendingAdvanceJob.cancel/resume вокруг открытия/закрытия.
57. **WordBlankChoices + LetterChoiceRow — кнопки букв вместо TextField** — в тренажёрах №9-12 русского `IosTextField` требовал открывать клавиатуру + тапать + закрывать. На мобильном UX медленный. Сейчас 2-3 кнопки 64dp с правильным ответом + одной альтернативой. **`data/WordBlankChoices.kt`** — `choicesFor(correctAnswer)` возвращает варианты на основе самого правильного ответа (т.к. в `WordBlank` нет `subtypeSlug`, только masked/answer/full/rule_hint). PAIRS-карта: а/о, е/и, у/ю, я/а, ы/и, ё/е, з/с, н/нн, ъ/ь — 15 ключей. Для редких ответов (`иги`, `ёвы`, `ице`, `вз`) возвращает один элемент — single button (тренажёр всё равно полезнее чем TextField). **`ui/common/LetterChoiceRow.kt`** — Row с равными кнопками 64dp через `Modifier.weight(1f)`. PressableSurface-like scale 0.96 с MutableInteractionSource + animateFloatAsState. Haptic LongPress на тапе. 4 состояния: Idle (BgElevated + Separator border), Selected (SystemBlue фон, белый текст), Correct (SystemGreenTint + green border), Wrong (SystemRedTint + red border). После Verdict кнопки disabled. **`WordBlankTrainerViewModel.checkLetter(letter)`** — комбинированный setInput+check за один вызов (экономит пользователю tap по «Проверить»). `LetterChoiceInputPanel` — новый composable в Screen с rule_hint показом при Wrong. Если в P5+ добавим парсер `extract_word_blanks.py` v2 с `subtypeSlug` — расширить PAIRS до per-subtype map (`kasn-kosn → [а, о]`).
58. **AppSettings.useLetterChoices toggle (default true)** — пользователь может вернуться к TextField через Настройки → Тренажёры → «🔤 Кнопки выбора букв» Switch. Default ON — это лучший mobile UX, но для редактирования через bluetooth-клавиатуру TextField может быть удобнее. Хранится в `AppSettingsStore` как `booleanPreferencesKey("use_letter_choices")`, восстанавливается из бэкапа в `restore()`. `WordBlankTrainerScreen` читает через `collectAsState` на `AppSettingsStore.settingsFlow(context)` — при смене toggle UI мгновенно перекрашивается без перезапуска экрана. **Это новая секция в SettingsScreen** — «Тренажёры» (между «AI помощник» и «Данные»), чтобы было место для будущих trainer-настроек (например, частота повторов слова, режим SRS в Phase 5).
59. **Два типа AI-лимитов: внутренний vs провайдерский** — в P4-A пользователи путались с сообщением «Превышен лимит». Сейчас: **(1) НАШ лимит** — `AiSettings.dailyLimit` default **200** (раньше 50 — слишком мало для активной подготовки), `todayUsage` в `AiSettingsStore` с auto-reset по дате. Проверка `canMakeRequest()` ДО HTTP-запроса. Message: «Достигнут твой дневной лимит (N/M). Увеличить или сбросить — в Настройках → AI → Лимит в день». **(2) ПРОВАЙДЕРСКИЙ лимит** — 429 от OpenRouter/Gemini/Anthropic. Message: «Превышен лимит провайдера. Попробуй через пару минут или переключись на другой провайдер в Настройках». Это `isRateLimit = true` ветка в `AiResponse.Error`, обрабатывается в `AskAiViewModel.askImpl` ОТДЕЛЬНО от прочих ошибок. **`AiSettingsStore.resetTodayUsage(context)`** — ставит `TODAY_USAGE = 0` + `TODAY_DATE = today`. Кнопка TertiaryButton «Сбросить счётчик сегодня» в `DailyLimitBottomSheet`. Sheet также показывает «Сегодня использовано: N из M» под крупной цифрой лимита. **Кеш-хит НЕ инкрементит todayUsage** — проверка cache идёт ПЕРЕД `canMakeRequest()`, если hit → return до increment. Только `is AiResponse.Success` после реального HTTP-запроса вызывает `incrementTodayUsage()`. **При добавлении новых провайдеров в P5+** — следить за discriminator: 401/403→isAuthError, 429→isRateLimit, 5xx→общая ошибка с providerLabel.
60. **Adaptive icon через Pillow-скрипт из PNG-исходника** — `parser/scrapers/generate_app_icons.py` берёт `app_icon_source.png` (произвольная картинка, обычно широкоформатная) и генерирует полный набор Android-иконок одной командой. Шаги: (1) **Background** 432×432 (4× xxxhdpi = 108dp) — квадратный кроп центра исходника + `ImageEnhance.Brightness(0.55)` + `Contrast(0.85)` для затемнения (foreground должен читаться на любой подложке). (2) **Foreground** 432×432 — кроп ~900×900 вокруг визуального центра «100» (cy на 52% высоты — лого исходника не геометрически по центру), ресайз в safe area 264×264dp (4× 66dp), помещён в прозрачный 432×432 canvas. (3) **Adaptive icon XML** в `mipmap-anydpi-v26/ic_launcher.xml` + `ic_launcher_round.xml` со ссылками на drawable PNG + `<monochrome>` для Android 13+ themed icons. (4) **Legacy mipmap** — flat composite `Image.alpha_composite(bg, fg)`, ресайз в 5 размеров (mdpi 48, hdpi 72, xhdpi 96, xxhdpi 144, xxxhdpi 192), + круглая маска через `ImageDraw.ellipse` для `ic_launcher_round.png`. (5) **Очистка** — `remove_old_icons()` удаляет все `mipmap-*/ic_launcher.*` (PNG + WEBP) перед записью новых. При смене иконки в Phase 5+ — заменить `app_icon_source.png` и перезапустить скрипт; XML и структура не трогаются. **Также удалить** `res/drawable/ic_launcher_foreground.xml` (старый Stage 5 vector concept) + `res/values/ic_launcher_background.xml` (старый color resource, конфликтует с новым drawable одноимённого названия).
61. **SmoothLazyColumn + frictionMultiplier 0.7 — iOS-style fling** — стандартный compose-fling использует Android-decay (резкий impulse + быстрая остановка), на устройстве с привычкой к iOS ощущается «дёрганым». `ui/common/SmoothLazyColumn.kt` определяет `rememberSmoothFlingBehavior(frictionMultiplier = 0.7f)` через `exponentialDecay<Float>(frictionMultiplier)` (0.7 < 1.0 default → меньше трения = длиннее инерция). Кастомный `SmoothFlingBehavior` extends `FlingBehavior` с `AnimationState.animateDecay` + `cancelAnimation` при упоре. `SmoothLazyColumn` — drop-in wrapper над `LazyColumn` с встроенным fling. **Применён в 17 экранах** (все LazyColumn кроме wheel-pickers в EditBottomSheets — там snap-логика, iOS fling не подходит): HomeScreen, CatalogScreen, TypesScreen, SubtypesScreen, ProblemListScreen, ProblemDetailScreen, AccentCategoriesScreen, JournalScreen, FavoritesScreen, ErrorsListScreen, StatsScreen, MockExamCalendarScreen, MockExamHistoryScreen, MockExamRunnerScreen, FipiVariantsScreen, ProfileScreen, SettingsScreen. **При добавлении новых LazyColumn в P5+** — использовать `SmoothLazyColumn` по умолчанию, кроме особых случаев (snap, paging с предсказуемым шагом). Если нужен кастомный fling — `LazyColumn(flingBehavior = rememberSmoothFlingBehavior(frictionMultiplier = X))`.
62. **SwipeBackContainer — iOS-style swipe-back с visual feedback** — `ui/common/SwipeBackContainer.kt` обёртка для detail-screens, заменяет старый `Modifier.edgeSwipeBack` (Convention #20, удалён) который был passive listener без visual. Сейчас: edge zone **24dp** (от левого края), при свайпе оттуда вправо контент следует за пальцем через `Animatable + graphicsLayer { translationX = offsetX.value }`, threshold **30% screenWidth**, после release → если >threshold → `animateTo(screenWidth)` + `onBack()` + `snapTo(0)`, иначе `animateTo(0, spring 0.85f)` (пружинит обратно). `detectHorizontalDragGestures` с `skip = startOffset.x > edgeWidthPx` чтобы свайпы из центра не срабатывали — там другой жест (SwipeableProblemContent для перехода между задачами). **Применяется per-screen в EgeApp.kt** — обернуть каждый `composable<XRoute>` блок (18 detail-routes: ErrorsListRoute, StatsRoute, FavoritesRoute, ProfileRoute, SettingsRoute, TypesRoute, SubtypesRoute, ProblemListRoute, ProblemDetailRoute, AccentCategoriesRoute, AccentTrainerRoute, WordBlankTrainerRoute, QuickTrainerRoute, MockExamCalendarRoute, MockExamDetailRoute, MockExamRunnerRoute, FipiVariantsRoute, MockExamHistoryRoute). НЕ обёрнуты root tabs (HomeStubRoute, CatalogRoute, JournalStubRoute) — там popBackStack() = no-op. Глобальный `Modifier.edgeSwipeBack` на NavHost удалён — теперь per-screen контроль (можно отключить через `enabled = false` если нужно, например в MockExamRunner для предотвращения случайного выхода).
63. **SwipeableProblemContent + резинка /3 + onboarding overlay** — горизонтальные свайпы между задачами/словами с visual feedback. `ui/common/SwipeableProblemContent.kt` принимает `hasPrev/hasNext/onPrev/onNext/onSwipeStart`. Threshold = **screenWidth × 0.25** (~100dp на 400dp экране). Visual через `Animatable<Float>` + `graphicsLayer { translationX = offsetX.value }`. **Резинка /3 на границах**: если `!hasNext` и пользователь свайпает влево — `effective = current + drag / 3f` (тянется только треть движения пальца). При release без триггера → spring обратно к 0. `skip = startOffset.x < edgePx (24dp)` чтобы edge swipe-back (Convention #62) сработал первым. **`onSwipeStart` callback** — в AccentTrainer/WordBlankTrainer вызывает `vm.onAskAiOpened()` чтобы отменить `pendingAdvanceJob` (Convention #56) — иначе после правильного ответа таймер 1сек переключит слово прямо во время свайпа. **Onboarding overlay** `SwipeHintsOverlay.kt` показывается один раз при `!swipeHintsShown && !hintsLocallyDismissed` (флаг в `AppSettings.swipeHintsShown: Boolean = false` default). 3 hint-row (👈 следующая, 👉 предыдущая, ↩️ от края назад) + кнопка «Понятно» → `markSwipeHintsShown(context)`. Backup v1.7 хранит флаг — `CURRENT_VERSION = "1.7"`, `SUPPORTED_VERSIONS = ["1.0".."1.7"]`. **При добавлении других swipe-экранов в P5+** (SRS карточки, AI-чат) — следовать тому же паттерну: SwipeableProblemContent + onSwipeStart hook для координации с другими async операциями.

---

## Safety Rules — шесть страховок из premortem (обязательные)

Эти правила не обсуждаются, они зашиты в архитектуру. Если возникает соблазн нарушить — **сначала спроси пользователя**.

### Rule 1 — Радар по подвидам, не по типам
Главный экран показывает ~50 секторов КЭС (тригонометрия, показательные функции, стереометрия и т.д.), **не** 19 (по номерам задач).
- Минимум **15 решённых задач** в подвиде для окрашивания сектора.
- Меньше — сектор серый, подпись «недостаточно данных».
- Это закрывает ложную уверенность из-за смещённой выборки (пользователь обычно решает любимые подвиды).

**Примечание после Stage 0 разведки (2026-05-16):**
- Для **математики** радар работает по `problem_subtypes.id` (~150 подвидов КЭС, есть в каталоге sdamgia).
- Для **русского** sdamgia не даёт семантической подкатегоризации внутри номера задачи (подвиды у русского — это источники: «Задания ФИПИ», «Задания демоверсий», «Задания для подготовки», «Задания тренировочных работ»). Поэтому для русского радар — по `problem_types.number` (27 секторов). Это сознательное упрощение; вернёмся к семантической подкатегоризации через Claude API только если радар русского окажется бесполезным на практике (Фаза 3+).

### Rule 2 — AI-замок на первой попытке
Кнопка «Спросить ИИ» на экране задачи **disabled**, пока пользователь не ввёл хотя бы один ответ (даже неправильный).
- В UI: «Сначала попробуй — потом спрашивай».
- Это закрывает риск AI как эмоциональной замены решения.

### Rule 3 — Календарь пробников зашит в app
16 пробников за год, **каждые 3 недели**. Первый — через **4 недели** после планируемого конца Фазы 5.
- На главном экране — счётчик «дней до следующего пробника».
- Дата хранится в БД, не в коде (пользователь может скорректировать).
- Это закрывает «год — это вечность».

### Rule 4 — Стресс-тест корпуса
В конце Фазы 1 (Stage 4 парсера): скачать **10 открытых вариантов КИМ ФИПИ 2026** в папку `parser/kim-fipi/`. Для каждой задачи проверить покрытие в `corpus.db` через FTS-поиск или семантический подбор.
- Покрытие <80% → парсер недотягивает, добирать.
- Это закрывает риск неполной/устаревшей базы.

### Rule 5 — Правило «50 задач в неделю»
В каждую неделю, в которой был хоть один git-коммит, в app должно быть решено **минимум 50 задач**.
- Реализация: в начале сессии Claude Code, если есть коммиты за последние 7 дней — проверить количество решённых задач в `user_progress` за тот же период.
- Меньше 50 → **код заморожен**: в UI Claude Code предупреждает пользователя и предлагает закрыть редактор.
- Это закрывает «sublime app, пустая голова».

### Rule 6 — Контрольная точка через 8 недель
Пользователь поставил себе в Google Calendar дату через 8 недель от старта Фазы 1.
- В этот день: проверить количество решённых задач в `user_progress`.
- Если <300 → переключение в pure-usage mode на месяц (никакого кода).
- Это закрывает медленную деградацию мотивации.

---

## Working with the user

- **Спрашивай, когда не уверен.** Особенно по: бюджету API, новым архитектурным решениям, удалению файлов, изменению структуры БД.
- **Не угадывай дедлайны фаз.** Если фаза затягивается — доложи и предложи варианты (сжать / упростить / отложить).
- **Один раз в начале фазы — стресс-тест.** В Фазе 1 это Stage 0 (разведка sdamgia). В Фазе 2 — тестовый запуск пустого Compose-проекта на эмуляторе.
- **Коммиты:** используй `git`. Каждый завершённый stage внутри фазы — один коммит. После полной фазы — тэг `phase-N-done`.
- **Логирование:** все сетевые запросы парсера логировать в `parser/logs/scraper.log` с timestamp. Это даёт пользователю доказательство «парсер шёл медленно, не делал ddos».

---

## Out of scope (не делать без явного запроса)

- Google Play, signing keys, ProGuard для релиза.
- Синхронизация между устройствами, бэкап в облако.
- Социальные функции (чаты, лидерборды, шаринг прогресса).
- Информатика как третий предмет (исключено осознанно).
- Базовая математика, биология, обществознание.
- Теория и курсы (app — про практику, теория — в учебниках/YouTube).
- **Дополнительные задания математики Д1..Д14 НЕ парсятся в Phase 1.** Причина: пользователь хочет тренироваться только на актуальном формате ЕГЭ-2026, чтобы избежать ложной уверенности от устаревших формулировок. Если Stage 4 покажет покрытие КИМ ФИПИ-2026 ниже 80% — рассмотрим добор Д-задач как fallback, но решение принимает пользователь, не Claude Code автономно.
- **Устаревшие задания русского (Д А7, Д А9, Д А10, Д А11, Д А12, Д А20, Д А24, Д В1-В7, а также «B13/B14/L. Великовой/C27» и прочие после разделителя) НЕ парсятся в Phase 1.** Причина та же: тренируемся только на актуальном формате ЕГЭ-2026. Маркер на sdamgia: всё, что в каталоге `/prob_catalog` идёт после узла `<h3>Дополнительные задания для подготовки</h3>` (в живом UI sdamgia может также показывать заголовок «Задания, не входящие в ЕГЭ этого года»). Парсер `parse_catalog` автоматически детектирует этот разделитель и помечает все типы после него как `is_supplementary=True`. Та же логика обхода работает для обоих предметов.

---

## Roadmap status

| Фаза | Описание | Статус |
|---|---|---|
| 1 | Парсер sdamgia → corpus.db (2 предмета) | ✅ **DONE 2026-05-17** (10272 задач, 36 правил, 100% покрытие КИМ ФИПИ-2026) |
| 2 | Android MVP — навигация, экран задачи, тренажёры, правила, избранное, светлая тема, иконка | ✅ **DONE 2026-05-18** (5 stages, APK 228 MB, тег `phase-2-done`) |
| 3 | Главный экран + Профиль + Настройки + Бэкап + Журнал + Пробники + Push + Страховки | ✅ **DONE 2026-05-19** (6 stages, APK 229.5 MB, тег `phase-3-done`) |
| 4 | AI-кнопка, генератор варианта, импорт КИМ ФИПИ, история пробников, иконка приложения, плавность скролла + свайп-жесты | ✅ **DONE 2026-05-19** (Stage A+B + C + C2 + C3, APK 231.2 MB, теги `phase-4-stage-c3-done` + `phase-4-done`). Краш приложения остаётся открытой проблемой — диагностика в P4-D2. |
| 5 | SRS по ошибкам-формулировкам | ⏸ Waiting |

### Stages внутри Фазы 4

| Stage | Описание | Статус |
|---|---|---|
| A+B | 3 AI-провайдера (OpenRouter/Gemini/Anthropic) + EncryptedSharedPreferences + кеш ответов + раздельные пробники math/rus + 6 эталонных КИМ ФИПИ + история с TrendChart + 3 quick fixes (INTERNET permission / OpenRouter free models rotation / AI context через Jsoup) | ✅ Done 2026-05-19, теги `phase-4-stage-a-done` + `phase-4-stage-b-done` |
| C | Финальная полировка (AnswerChecker для рус-вариантов, LatexCleaner + усиленный промпт, размер формул, NavHost spring 0.85f, «Проверить→Далее», AI в тренажёрах, trainerWordsLearned, 5xx error handling, Backup v1.6) | ✅ Done 2026-05-19, тег `phase-4-stage-c-done` |
| C2 | Мини-стейдж после теста P4-C: defensive try/catch + global crash handler, pendingAdvanceJob (AI закрывает auto-advance), кнопки букв №9-12 + toggle, 2 типа AI-лимитов с reset | ✅ Done 2026-05-19, тег `phase-4-stage-c2-done` |
| C3 | Иконка приложения (Pillow генератор) + SmoothLazyColumn везде + SwipeBackContainer (edge 24dp + visual) + SwipeableProblemContent (резинка /3 + onboarding) + Backup v1.7 | ✅ Done 2026-05-19, теги `phase-4-stage-c3-done` + `phase-4-done` |

### Известная нерешённая проблема — Phase 4 Stage D2 (диагностика крашей)

- **Краш приложения остаётся после P4-C2/C3 fixes.** Несмотря на `EgeApplication.installCrashHandler` + try/catch в hot paths (AnswerChecker / LatexCleaner / AskAiViewModel.ask), пользователь продолжает наблюдать «приложение остановлено». Stage P4-C2 защитил от исключений в новых файлах, но не покрыл всё (возможные кандидаты: EncryptedSharedPreferences/Tink с проблемой Keystore на конкретной версии Samsung One UI; OutOfMemoryError на 192 MB corpus.db в loop; race между ViewModel.viewModelScope и Activity recreation; Jsoup.parse на невалидном HTML из corpus.db). **План для P4-D2**: (a) подключить Samsung по USB или включить wireless debugging → `adb logcat -s EgeApp AndroidRuntime:E -d -t 1000 > crash.log` после реального краша. (b) Если adb недоступен — добавить `data/CrashLog.kt` который пишет stack trace в файл (`cacheDir/last_crash.log`), доступный через экспорт в Telegram. (c) В UI добавить «Отправить отчёт о краше» в Настройках → если файл существует, показать кнопку.

### Stages внутри Фазы 3

| Stage | Описание | Статус |
|---|---|---|
| A | Polish-долги из Phase 2 + 4-й таб Профиль + Настройки (тема/радар/уведомления) + Бэкап (экспорт/импорт через share-sheet + Auto Backup + Reset) | ✅ Done 2026-05-18, тег `phase-3-stage-a-done` |
| B | Главный экран — цитата дня, предиктор балла, радар (4 стиля), streak, быстрый тренажёр | ✅ Done 2026-05-18, тег `phase-3-stage-b-done` |
| C | Полноценный журнал — ошибки, статистика, user_attempts, экспорт CSV | ✅ Done 2026-05-18, тег `phase-3-stage-c-done` |
| C2 | Мини-правки: спойлер ответов в ErrorCard + опечатка «неперерешённых» + единый NotEnoughDataHint в радаре | ✅ Done 2026-05-18, тег `phase-3-stage-c2-done` |
| D | Финал Phase 3: календарь пробников + WorkManager push + SafetyGuards #5/#6 + Backup v1.3 + fix typesCovered + полировка | ✅ Done 2026-05-19, теги `phase-3-stage-d-done` + `phase-3-done` |

### Known issues для Phase 4

- **MockExamRunner.composeMix смешанный.** Сейчас при «Начать пробник» сразу подбирается 8 math + 8 rus вперемешку. Правильно: BottomSheet выбора предмета (Математика 19 заданий / Русский 27 заданий) → отдельные `composeMockMath()` (19 задач — по одной случайной из каждого типа 1..19) и `composeMockRus()` (27 задач). Перерабатывается в Stage P4-A.


### Stages внутри Фазы 2

| Stage | Описание | Статус |
|---|---|---|
| 1 | Каркас Android-проекта (Compose + Room/SQLite + Coil + Navigation + corpus.db как asset) | ✅ Done 2026-05-17, тег `phase-2-stage-1-done` |
| 2 | Каталог задач (предмет → тип → подвид → задача) | ✅ Done 2026-05-17, тег `phase-2-stage-2-done` |
| 3 | Экран задачи (условие, формулы, ответ, проверка, решение) + Apple-стиль + тренажёр №4 ударений | ✅ Done 2026-05-18, тег `phase-2-stage-3-done` |
| 4 | Polish 4 (адаптивный размер формул + FlowRow слогов) + тренажёры орфографии №9-12 русского | ✅ Done 2026-05-18, тег `phase-2-stage-4-done` |
| 5 | Память тренажёров + edge swipe + 46 правил + избранное + свайпы между задачами + светлая тема + иконка | ✅ Done 2026-05-18, тег `phase-2-stage-5-done` + `phase-2-done` |

### Known issues для Phase 3 (отложено осознанно после Stage 5)

- **Прогресс-бар в светлой теме плохо виден.** Фон бара захардкожен `Color(0x33FFFFFF)` в AccentTrainerScreen + WordBlankTrainerScreen — в светлой теме на белом фоне он бледно-серый. Фикс: использовать `Separator` (динамический) или `LabelTertiary.copy(alpha = 0.2f)`.
- **Шапка `WordBlankTrainer` не унифицирована с `LargeTitleBar`.** Используется одинаковая структура, но edge swipe back в WordBlank-тренажёре не отрабатывает (видимо, IosTextField перехватывает pointer events до того как edge swipe их увидит). Решение для Phase 3: либо вынести edge swipe из NavHost на каждый экран отдельно, либо обернуть текстовое поле в pointerInput-pass-through.
- **Плавность горизонтальных свайпов между задачами/словами.** Сейчас `detectHorizontalDragGestures` срабатывает по `dragEnd` с проверкой `totalDrag > triggerPx` — это резкое переключение без визуального follow-trace. Хотелось бы видеть «карту» текущей задачи, ползущую за пальцем, как в iOS. Решение: переписать через `Animatable` + `AnimatedContent` с offset trace или использовать `Pager` из accompanist.

**Stage 1 итоговые метрики (2026-05-17):**
- Создан `android/` проект: package `com.daniel.ege100`, app name `EGE100`, minSdk 26, targetSdk 36.
- Стек: Kotlin 2.0.21 + Compose BOM 2024.12.01 + Material 3 + Navigation 2.8.5 + Room 2.6.1 + Coil 2.7.0 + Ktor 3.0.3 (пустой, под Phase 4). AGP 8.7.3, Gradle 8.11.1.
- `corpus.db` (192 MB) скопирована в `android/app/src/main/assets/` — gitignored, локальная копия из `parser/corpus.db`.
- Подключение БД на этом этапе — **через raw SQLite + ручное копирование asset в databases/**, а не через Room.createFromAsset. Причина: corpus.db собрана `build_db.py` со своим набором колонок/индексов, Room строго валидирует @Entity ↔ таблица. Объявлять все 10+ Entity ради одного `SELECT COUNT(*)` в Stage 1 — преждевременно. Зависимость `androidx.room:room-runtime` остаётся подключённой, полноценные Entity/Dao добавим в Stage 2.
- В Stage 1 одна Activity, один экран: тёмная тема, центрированный текст «БД подключена / Задач в корпусе: 10272». Smoke-тест на самом устройстве — на стороне пользователя (Samsung Galaxy через Telegram, USB-отладки нет).
- APK: `android/app/build/outputs/apk/debug/app-debug.apk` — **204 MB**, в пределах ожидаемого (192 MB БД + Compose runtime + Coil/Ktor/Room рантаймы).
- `aapt noCompress += "db"` в `app/build.gradle.kts` — не сжимаем sqlite в APK (раздувание сжатого .db ломает копирование на части устройств).
- Тёмная тема только (Light палитра в Theme.kt задана на будущее, но фактически приложение принудительно тёмное на этом этапе — упрощает первый запуск, согласно DESIGN_SPEC.md §2 «только тёмную пока»).

После завершения каждой фазы — обновить эту таблицу в `CLAUDE.md`. Помечать ✅ Done.

### Stages внутри Фазы 1

| Stage | Описание | Статус |
|---|---|---|
| 0 | Разведка sdamgia (10/10 запросов, selectors.yaml) | ✅ Done 2026-05-16 |
| 1 | Парсер математики профильной (только основные №1..№19, 4863 задачи) | ✅ Done 2026-05-17, тег `phase-1-stage-1-done` |
| 2 | Парсер русского №1..№27 + extract_rules.py (5409 задач, 36 правил) | ✅ Done 2026-05-17, тег `phase-1-stage-2-done` |
| 3 | `build_db.py` → corpus.db + FTS5 + view_corpus.html | ✅ Done 2026-05-17, тег `phase-1-stage-3-done` |
| 4 | Стресс-тест по КИМ ФИПИ-2026 (Safety Rule #4) | ✅ Done 2026-05-17, тег `phase-1-stage-4-done`, **100% покрытие** |

**Stage 4 итоговые метрики (2026-05-17, Safety Rule #4 закрыт):**
- Источник: `parser/kim-fipi/{math_profile,russian}_demo_2026.pdf` (демоверсии ФИПИ-2026, скачаны с doc.fipi.ru, gitignored).
- Извлечение: 19 задач math (со всеми «ИЛИ»-вариантами) + 27 задач rus = **46 задач КИМ**.
- Метод: 2-колоночный crop PDF → split по номеру → split по «ИЛИ» → FTS5-запрос (топ-8 длинных русских слов с OR) → если хотя бы один variant находит матч в нужном предмете, задача считается покрытой.
- Результат: **math 19/19 (100%), rus 27/27 (100%), overall 46/46 (100%)**. Порог 80% закрыт с большим запасом.
- Качество спот-проверено: №16 «Кредит» → точный аналог в corpus (sdamgia_id 660700), №5 «Паронимы» → точное совпадение текста (sdamgia_id 59670), №1 планиметрия → корректные матчи на тип «Четырёхугольник, вписанный в окружность».
- Claude API **не понадобился**: 100% покрытие через FTS5. Бюджет $0 из доступных $3.
- Артефакт `parser/coverage_report.md` (104 KB, gitignored) — детальный отчёт с FTS5-запросами и top-3 матчами на каждый variant.

**Supplementary Д1..Д19 математики (~2986 задач) НЕ добирались** — раз основные №1..№19 дали 100% покрытие демо-варианта, эти типы не нужны. Решение зафиксировано в Out of scope.

## Phase 1 итог

Парсер sdamgia → corpus.db (2 предмета) завершён.

- **corpus.db 192 MB** на диске: 10272 задачи (4863 math + 5409 rus), 36 уникальных правил, 5288 привязок задача↔правило, FTS5-индекс по statement_html.
- **0 ошибок** во всех 4 стейджах. **0 банов** от sdamgia. **0 4xx/5xx**.
- Wall time всего парсинга: math ~14ч (с простоями) + rus 1.6ч + build_db 6с + Stage 4 ~30с.
- Артефакты на диске (gitignored): math.jsonl 46 MB, russian.jsonl 138 MB, russian_rules.jsonl 1.1 MB, parser/assets/ 308 MB (формулы + иллюстрации), parser/cache/raw/ 1.2 GB.
- Inventory кода в репо: `parser/pipeline/`, `parser/scrapers/{sdamgia_recon,math_profile,russian,extract_rules,fix_illustration_extensions,stage4_coverage,probe_cat292}.py`, `parser/build_db.py`, `parser/export_view.py`, `parser/tests/test_smoke.py` (32/32 passed), `parser/selectors.yaml`.

Готов к Phase 2 (Android MVP по `DESIGN_SPEC.md`).

**Stage 3 итоговые метрики (2026-05-17):**
- `parser/corpus.db` 192.27 MB, собирается за 6 секунд.
- 2 subjects, 46 problem_types, 223 problem_subtypes, **10272 problems**, 10272 solutions, 36 rules, 5288 problem_rules, 10272 entries в FTS5.
- Integrity: `PRAGMA integrity_check = ok`, `PRAGMA foreign_key_check = 0 issues`, 0 orphan solutions, 0 orphan problem_rules.
- Распределение: mathb 4863 (100%) + rus 5409 (100%).
- 9 коллизий sdamgia_id между math и rus (исторические низкие ID 902-915) — решено через `UNIQUE(subject_id, sdamgia_id)` вместо `sdamgia_id UNIQUE`. Расхождение со старой схемой зафиксировано в CLAUDE.md.
- `view_corpus.html` 833 KB — выборка 10 math + 10 rus с формулами/иллюстрациями/правилами. Открывается двойным кликом из `parser/`.

**Stage 2 итоговые метрики (2026-05-17):**
- 5409 / 5409 задач русского (100% покрытие main №1..№27), 101 / 101 подвидов, 0 ошибок.
- 1123 HTTP-запроса: 1123 успешных, 0 timeout, 0 4xx, 0 5xx, 0 банов.
- Wall time: 97.3 минут (1.6ч). Темп ~55 задач/мин — заметно быстрее математики из-за минимума формул в русском.
- 36 уникальных правил, 5288 привязок задача↔правило. Дедупликация ~147 задач на правило в среднем (max 325 для правила про сложносочинённое предложение).
- 121 задача без правила — это №1-3, у которых на sdamgia не выводится исходный текст (известное ограничение, см. Stage 0 анализ).
- Артефакты: `russian.jsonl` 138 MB, `russian_rules.jsonl` 1.1 MB, `russian_problem_meta.jsonl` 590 KB.
- Smoke: 32/32 зелёные (включая стресс на №5 паронимы).

**Stage 1 итоговые метрики (2026-05-17):**
- 4863 / 4863 задач (100% покрытие main математики), 146 / 146 подвидов.
- 19 типов ЕГЭ покрыты полностью (от №1 «Планиметрия» до №19 «Числа и их свойства»).
- 52 697 уникальных SVG-формул (дедупликация по хешу), 3139 иллюстраций для 2637 задач.
- 0 ошибок в `state.errors`. Из 1049 HTTP-запросов: 1040 успешных + 9 transient timeout, все retried OK. Банов / 4xx / 5xx — 0.
- Wall time: ~14ч (включая простой между сессиями), чистый сетевой бюджет ~8ч.
- Артефакты на диске: `parser/math.jsonl` 46 MB, `parser/cache/raw/` 345 MB (1038 HTML), `parser/assets/` 308 MB (формулы + иллюстрации).

**Решение по supplementary (2026-05-16):** в Stage 1 берём только основные №1..№19 математики (4863 задачи, ~1 час прогона), supplementary Д1..Д19 (ещё 2986 задач) откладываем до Stage 4. Логика: если покрытие КИМ ФИПИ-2026 ≥80% без supplementary — они не нужны (это «старая нумерация», частично пересекается с основными). Если <80% — доберём именно те Д-типы, которые покрывают пробелы. Триггер: stop-сигнал «объём >5000» сработал на 7849 задач (4863+2986).

---

## Tech stack — заметки после Stage 0 разведки

Изменения в архитектурных решениях по итогам разведки sdamgia 2026-05-16.

### Формулы — НЕ MathML, а SVG-картинки
**Что обнаружено:** sdamgia рендерит формулы на сервере и отдаёт как `<img class="tex">` с URL вида `https://ege.sdamgia.ru/formula/svg/{2hex}/{32hex}.svg` (контент-адресуемое хранилище, картинки дедуплицированы по хешу). Атрибут `alt` содержит **транскрипцию формулы на русском** («дробь: числитель: 4, знаменатель: 7 …»).

**Последствия:**
- На Android — отображение через AsyncImage / Coil. MathJax 4 в WebView **не нужен**. Это сильно упрощает Фазу 2 (меньше JS-инфраструктуры, нет проблем с производительностью WebView на старых телефонах).
- Парсер скачивает SVG в `assets/{sdamgia_id}/formulas/{hash}.svg`, в JSONL хранит относительные пути.
- В FTS5-индекс (Stage 3) попадает alt-текст формул вместе с обычным текстом условия — это бесплатный bonus к качеству поиска.
- Для Claude API (Stage 4 покрытие КИМ ФИПИ): alt-текст пригоден напрямую, ничего конвертировать не надо.

### React-SPA на корне поддоменов — НЕ наш интерфейс
Корни `https://math-ege.sdamgia.ru/` и `https://rus-ege.sdamgia.ru/` отдают пустой React-SPA shell (8 КБ, `<div class="Root">` + бандлы `static/js/main.*.chunk.js`).

**Парсим не SPA, а старый PHP-движок** на тех же поддоменах:
- `/prob_catalog` — дерево типов/подвидов (статичный HTML, 228 КБ math / 393 КБ rus).
- `/test?filter=all&category_id=N&page=K` — список задач подвида, **с полными условиями + решениями + ответами + КЭС-кодами**. Пагинация через `data-total`/`data-page`.
- `/problem?id=N` — одна задача (для smoke-тестов; штатный обход через `/test`).

Все три endpoint-а возвращают серверный HTML без JS-рендера. Парсятся httpx + selectolax. Cloudflare Turnstile в инфраструктуре есть, но при разведке (10 запросов) не активировался.

### Стратегия обхода Stage 1 — через /test, не /problem
В каждой странице `/test` отдаются полные задачи, не превью. Это значит:
- ~150 подвидов × среднее 2-4 страницы на подвид ≈ **~400-600 запросов** для всей математики, а не 2-4 тыс. как было бы при пообходе через `/problem?id=N`.
- При rate-limit 1.5-2с + jitter — это ~15-25 минут чистого обхода, не часы.
- Резервный `/problem?id=N` используется только для задач, которые упоминаются в каталоге, но не попали ни в один `/test` (теоретическая ситуация — проверим на Stage 1).

---

## Fallback plans

Процедуры на типовые аварийные ситуации с парсером. Если в будущей сессии случилась одна из них — следовать пошагово, не импровизировать.

### A. Бан от sdamgia (HTTP 403/429 устойчиво)

Если при работе парсера sdamgia возвращает 403/429 и exponential backoff (1, 2, 4, 8с в `fetch.py`) не помог в течение **>30 минут**:

1. **Остановить парсер.** Background-task через `TaskStop`. State.json уже сохраняется атомарно каждые 50 задач и при `mark_subtype_done` — последний чекпоинт безопасен.
2. **Спросить пользователя:** «Заходит ли sdamgia.ru из обычного браузера на этой машине прямо сейчас?»
3. **Если нет — IP забанен.** Варианты, которые предлагаешь пользователю (но **не выбираешь сам**):
   - а) Подождать 12-24 часа — у sdamgia баны обычно временные.
   - б) Перезагрузить роутер для смены динамического IP (если провайдер выдаёт динамику).
   - в) Включить VPN.
   - г) Переключиться на план B — открытый банк ФИПИ (см. раздел B ниже).
4. **Решение принимает пользователь, не Claude Code автономно.** Это не баг для самостоятельного фикса, а инфраструктурный выбор с компромиссами (время vs стоимость VPN vs изменение архитектуры парсера).
5. **При возобновлении после паузы** — запускать `python parser/scrapers/math_profile.py` как обычно. Восстановление работает само:
   - `state.completed_subtypes` пропускает уже завершённые подвиды.
   - `parser/cache/raw/` хранит все ранее скачанные `/test`-страницы — повторных сетевых запросов на них не будет (`fetch.py` сначала проверяет кеш).
   - `JsonlWriter.has(sdamgia_id)` пропускает уже сохранённые задачи внутри незавершённого подвида.
   - `parser/assets/_formulas/{hash}.svg` тоже не перекачивается, если файл уже на диске.

### B. Переход на открытый банк ФИПИ

Когда применять:
- Sdamgia блокирует парсер на >2 дня подряд (после всех вариантов из секции A).
- На sdamgia сменилась разметка и чинить `selectors.yaml` дольше суток.

В `parser/fipi-recon-archive/` лежат файлы разведки ФИПИ из старой чатовой беседы:
- `fipi_recon.py`, `fipi_recon2.py` — рабочие скрипты, тащат HTML с `ege.fipi.ru/bank/`.
- `recon/`, `recon2/` — скачанные HTML-страницы для анализа структуры.

Что в ФИПИ:
- Структура URL: `https://ege.fipi.ru/bank/questions.php?proj={PROJ_ID}&page={N}&pagesize=100`.
- PROJ_ID для математики профильной: `AC437B34557F88EA4115D2F374B0A07B`.
- PROJ_ID для русского: `AF0ED3F2557F8FFC4C06F80B6803FD26`.
- Кодировка: windows-1251 → сохранять `r.content` как байты, не `r.text`.
- SSL: `verify=False` (у ФИПИ сломана цепочка сертификатов, для GET публичного HTML — безопасно).
- В HTML нет правильных ответов — проверка через POST на `solve.php` или однократное прогона через Claude API.

---

## Budget & API limits

- **Claude API hard limit:** $15/мес (выставляется в `console.anthropic.com`).
- **Дефолтная модель в app:** `claude-haiku-4-5-20251001` (дёшево).
- **Опциональная:** `claude-sonnet-4-7` по запросу пользователя (флаг «Подробно» в UI).
- **Кеш ответов:** обязателен. `prompt_hash = sha256(problem_id + normalize(question))`. Тот же вопрос второй раз — из БД, токены не тратятся.
- **Индикатор стоимости:** в шапке Settings — «Потрачено в этом месяце: $X.YZ / $15».

---

## Last update

2026-05-19 — **Phase 4 ✅ DONE** (теги `phase-4-stage-c3-done` + `phase-4-done`). Финальная косметика — иконка, плавность скролла, swipe-жесты, onboarding. **Часть А (иконка, Convention #60):** `parser/scrapers/generate_app_icons.py` через Pillow: исходник `app_icon_source.png` (2816×1536, золотое «100» на тёмно-синем созвездном фоне) → background 432×432 (центр-кроп + Brightness 0.55 + Contrast 0.85), foreground 432×432 (центральная зона ~900×900 вокруг cy=52%h, ресайз в safe area 264×264, прозрачный 432×432 canvas), `mipmap-anydpi-v26/ic_launcher.xml` + round с `<monochrome>`, 5 legacy mipmap размеров (mdpi 48 / hdpi 72 / xhdpi 96 / xxhdpi 144 / xxxhdpi 192) square + round (через `ImageDraw.ellipse` mask). Удалены: `res/drawable/ic_launcher_foreground.xml` (Stage 5 vector concept), `res/values/ic_launcher_background.xml` (старый color resource — конфликт имён), все старые `mipmap-*/ic_launcher.*` PNG/WEBP. **Часть Б (SmoothLazyColumn, Convention #61):** `ui/common/SmoothLazyColumn.kt` с `exponentialDecay(frictionMultiplier = 0.7f)` (vs Android default 1.0). Кастомный `SmoothFlingBehavior` через `AnimationState.animateDecay` + `cancelAnimation` при упоре. Drop-in замена LazyColumn в 17 экранах через sed-скрипт: HomeScreen, CatalogScreen, TypesScreen, SubtypesScreen, ProblemListScreen, ProblemDetailScreen, AccentCategoriesScreen, JournalScreen, FavoritesScreen, ErrorsListScreen, StatsScreen, MockExamCalendarScreen, MockExamHistoryScreen, MockExamRunnerScreen, FipiVariantsScreen, ProfileScreen, SettingsScreen. EditBottomSheets (wheel-pickers) пропущен — там snap-логика. **Часть В1 (SwipeBackContainer, Convention #62):** `ui/common/SwipeBackContainer.kt` с edge zone 24dp + `Animatable<Float>` + `graphicsLayer { translationX = offsetX.value }` для visual feedback. Threshold = 30% screenWidth → `animateTo(screenWidth) → onBack() → snapTo(0)`; иначе spring 0.85f обратно к 0. **Удалён** старый global `Modifier.edgeSwipeBack` на NavHost (Convention #20). Применён per-screen через Python-скрипт к 18 detail-routes (root tabs Home/Catalog/Journal — НЕ обёрнуты). Удалена папка `ui/modifiers/EdgeSwipeBack.kt`. **Часть В2 (SwipeableProblemContent + onboarding, Convention #63):** `ui/common/SwipeableProblemContent.kt` с threshold screenWidth × 0.25, **резинка /3** на границах (`!hasNext` → `effective = current + drag/3`), spring 0.85f обратно при release без триггера. `skip = startOffset.x < edgePx (24dp)` для координации с SwipeBackContainer. Параметр `onSwipeStart: () -> Unit` — в AccentTrainer/WordBlankTrainer вызывает `vm.onAskAiOpened()` (отменяет pendingAdvanceJob — иначе свайп переключал бы слово через 1сек после правильного ответа). Подключено в ProblemDetailScreen (заменил Stage 5 pointerInput) + обоих тренажёрах. **Onboarding** — `SwipeHintsOverlay.kt` с тремя 👈/👉/↩️ hint-row + кнопка «Понятно». `AppSettings.swipeHintsShown: Boolean = false` default + `markSwipeHintsShown()` + restore. Показывается один раз при первом открытии ProblemDetail. **Backup v1.7:** `CURRENT_VERSION = "1.7"`, `SUPPORTED_VERSIONS = ["1.0".."1.7"]`. Поле `AppSettings.swipeHintsShown` опциональное — старые бэкапы парсятся OK через default + `ignoreUnknownKeys`. **APK 231.2 MB** (+0.4 MB к C2 — иконка в 5 размерах × 2 варианта + adaptive PNG). Conventions #60-63 зафиксированы. **Phase 4 закрыта полностью** — теги `phase-4-stage-c3-done` + `phase-4-done`. **Известная нерешённая проблема**: краш приложения продолжает повторяться несмотря на P4-C2 crash handler + try/catch. Stage P4-D2 будет начинаться с диагностики (CrashLog.kt с записью stack trace в файл cacheDir/ для экспорта через Telegram, кнопка «Отправить отчёт о краше» в Настройках).

2026-05-19 — **Phase 4 Stage C2 ✅ DONE** (тег `phase-4-stage-c2-done`). Мини-стейдж: 4 правки после теста P4-C, главная — критичный фикс краша. **Часть Г (defensive coding, Convention #55):** stack trace получить не удалось (adb работает в `C:/Users/Daniel/AppData/Local/Android/Sdk/platform-tools/`, но Samsung без USB-отладки — передача через Telegram). Аудит hot paths из P4-C показал: `AnswerChecker` сигнатура `String, String` — не защищена от null'ов через цепочку рефакторингов; `LatexCleaner` — regex без try/catch на потенциально гигантском input; `AskAiViewModel.ask()` — no try/catch вокруг корутины; глобального handler'а непойманных исключений нет. Применены 4 уровня защиты: `AnswerChecker.isCorrect(userAnswer: String, correctAnswer: String?, format: String?)` — сигнатура расширена до String?, вся логика в `checkImpl` + try/catch с fallback на `equals(ignoreCase=true)`. `LatexCleaner.clean()` — try/catch вокруг regex-цепочки, fallback = вернуть исходный текст. `AskAiViewModel.askImpl()` — выделено в private suspend fun, обёрнуто в try/catch, при exception state.error = «Внутренняя ошибка: ${message?.take(120)}». `EgeApplication.installCrashHandler()` устанавливает `Thread.setDefaultUncaughtExceptionHandler` который пишет в `Log.e("EgeApp", ...)` ПЕРЕД передачей системному handler'у — при следующем падении и подключении adb: `adb logcat -s EgeApp AndroidRuntime:E -d -t 200`. **Часть А (pendingAdvanceJob, Convention #56):** в обоих trainer ViewModel'ях `private var pendingAdvanceJob: Job? = null` + `scheduleAutoAdvance(delayMs)` (отменяет старый, запускает новый) + `onAskAiOpened()` (cancel) + `onAskAiClosed(wasRight)` (если wasRight → scheduleAutoAdvance(500L)). Триггер `scheduleAutoAdvance(1000L)` в `tapSyllable`/`check` ViewModel'я при `Verdict.isRight`. Из Screen'а удалён LaunchedEffect с `delay(1000L) → vm.goNext()`. В Screen остался только `LaunchedEffect(showAi) { if (showAi) vm.onAskAiOpened() }` + `AskAiBottomSheet.onDismiss = { ...; vm.onAskAiClosed(verdict.isRight) }`. **Часть Б (кнопки букв, Conventions #57-58):** `data/WordBlankChoices.kt` — `choicesFor(correctAnswer)` с PAIRS-картой (15 ключей: а/о, е/и, у/ю, я/а, ы/и, ё/е, з/с, н/нн, ъ/ь); для редких ответов (`иги`, `ёвы`, `ице`, `вз`) — `[correctLetter]` single button. `WordBlank` не содержит `subtypeSlug`, поэтому fallback по букве — компромисс между spec'ом (per-subtype map) и текущей структурой данных. `ui/common/LetterChoiceRow.kt` — Row из 2-3 кнопок 64dp с равной шириной (`Modifier.weight(1f)`). PressableSurface-like scale 0.96 spring + haptic LongPress. 4 состояния: Idle/Selected (SystemBlue)/Correct (SystemGreenTint)/Wrong (SystemRedTint). `WordBlankTrainerViewModel.checkLetter(letter)` — комбинированный setInput+check. `WordBlankTrainerScreen` читает `AppSettingsStore.settingsFlow().collectAsState()` и через `useLetterChoices` выбирает между новым `LetterChoiceInputPanel` (с rule_hint показом при Wrong) и старым `InputPanel` с IosTextField. `AppSettings.useLetterChoices: Boolean = true` + DataStore key + restore в backup. Новая секция «Тренажёры» в SettingsScreen с SwitchRow «🔤 Кнопки выбора букв». **Часть В (2 типа AI-лимитов, Convention #59):** default `dailyLimit` 50 → **200** (50 было слишком мало для активной подготовки). `AiSettingsStore.resetTodayUsage(ctx)` ставит `TODAY_USAGE = 0` + `TODAY_DATE = today`. `AskAiViewModel.askImpl` различает: наш лимит → «Достигнут твой дневной лимит (N/M). Увеличить или сбросить в Настройках» (через canMakeRequest ДО HTTP); провайдерский 429 → «Превышен лимит провайдера. Попробуй через пару минут или переключись на другой провайдер» (через `if (resp.isRateLimit)` в AiResponse.Error ветке). Все `return@launch` заменены на `return` после выноса логики в `askImpl`. Cache hit НЕ инкрементит todayUsage — проверка cache идёт ПЕРЕД canMakeRequest, hit → return до increment. `DailyLimitBottomSheet` расширен: параметр `todayUsage: Int` + `onResetTodayUsage: () -> Unit`. Под крупной цифрой лимита: «Сегодня использовано: N из M». TertiaryButton «Сбросить счётчик сегодня» рядом с «Отмена». Описание теперь явно говорит «это твой внутренний лимит, лимит провайдера — отдельный, у OpenRouter ≈ 200/день, у Gemini ≈ 1500/день». **APK 230.8 MB** (без роста к P4-C). Conventions #55-59 зафиксированы. **Phase 4 НЕ закрыта** — открыт Stage C3 с known issues: иконка приложения (пользователь дал PNG) + плавность скролла/свайпа.

2026-05-19 — **Phase 4 Stage C ✅ DONE** (тег `phase-4-stage-c-done`). Финальная полировка Phase 4 — 6 частей в одной сессии. **Часть А (AnswerChecker, Convention #48):** новый `data/AnswerChecker.kt` решает критический баг — старые `matchesAnswer`/`matches` для `alternatives` дробили ответ по **пробелу**, а sdamgia использует **`|`**: ни «красивая», ни «красива» для `"красивая|красива"` не принимались. SQL-разведка по corpus.db показала 4 формата: `number`/`string`/`alternatives`/`multipart`/null. `isCorrect(userAnswer, correctAnswer, answerFormat)` нормализует (lowercase + `,→.` + collapse whitespace + trimEnd('.')), дробит по `|;`, делает прямое сравнение + multipart set-equality + цифровой permutation fallback. `hasMultipleVariants()` для UI-подсказки «💡 Можно ввести любой из правильных вариантов» под полем (показывается когда есть `|` или `format ∈ {alternatives, multipart}`). Подключено в `ProblemDetailViewModel.check`, `MockExamRunnerViewModel.check`, `WordBlankTrainerViewModel.check`. AccentTrainer — тап-based, не используется. **Часть Б (LatexCleaner + системный промпт, Convention #49):** двойная защита от LaTeX в AI-ответах. Системный промпт `EGE100_SYSTEM_PROMPT` в `ai/AiProvider.kt` усилен жёстким «КРИТИЧНО — НИКОГДА не используй LaTeX» + 6 правил «как надо» (дроби через `/`, корни через `√`, греческие юникодом) + список запрещённых символов `\ $ { }` + пример решения. Не `const val` (нельзя trimIndent в const) — обычный top-level `val`. Fallback-чистка `ai/LatexCleaner.kt`: `\(...\)`/`\[...\]`/`$...$` → текст; `\frac{a}{b}` → `a/b` (3 прохода для вложенных + упрощение `(4)/(7)` → `4/7`); `\sqrt{x}` → `√x`; `x^{2}`/`_{i}` → `x^2`/`_i`; 36 греческих букв + 21 матсимвол (≤≥≠≈→∞·×÷±…∈∉⊂∪∩∀∃); остаточные `\command` и `{}` удаляются; тройные newlines → двойные. Применяется ОДИН РАЗ в `AskAiViewModel.ask()` ПЕРЕД записью в кеш — повторный запрос получает уже-очищенный текст. **Часть В (размер формул, Convention #50):** в `HtmlRenderer` inline `scaleFactor 1.4 → 1.6`, минимум 20sp → 28sp; block-формулы `effectiveHeight = clamp(naturalHeight × 1.4, 48dp, 120dp)` (раньше «4/7 · x = 7 3/7» рендерилась 24dp — крошечно); иллюстрации `heightIn(min = 120.dp, max = blockMaxHeight)` — маленькие чертежи больше не выглядят обрезком. **Часть Г (NavHost + Проверить→Далее, Conventions #51-52):** `NAV_SPRING.dampingRatio` `DampingRatioNoBouncy (1.0)` → `0.85f` — лёгкая пружина iOS-style. В `ProblemDetailScreen` основная PrimaryButton при `CheckResult.Correct + hasNext` показывает «Далее →» с `vm.goNext()` (палец не двигается); при `Correct + !hasNext` → «Готово»; при `userAnswer.isBlank()` → «Показать решение»; иначе → «Проверить». **Часть Д (AI в тренажёрах, Convention #53):** `AskAiBottomSheet` получил параметр `customQuickQuestions: List<QuickQuestion>?` (data class с label+question). Если null — дефолтные 4 чипа задач. AccentTrainer и WordBlankTrainer передают свои 4: «Почему этот слог/буква?», «Какое правило?», «Похожие слова», «Запомнить» (мнемоника) — каждый с embedded `${word.word}` в question. SecondaryButton «🤖 Спросить ИИ» появляется в trainer body после Verdict. Контекст: «Слово: $word. Правильное ударение/буква: $correct. Пользователь ввёл: $user». `onOpenAiSettings → SettingsRoute` прокинут через EgeApp. **Часть Е (Convention #54):** **(1)** `UserStatsStore.incrementTrainerWordsLearned()` + `getTrainerWordsLearned()` + `trainerWordsLearnedFlow()`. Зовётся в обоих тренажёрах **только при isRight=true** рядом с `recordAttempt`/`StreakStore.onProblemSolved`/`attempt_log.insert`. `StatsScreen` теперь читает счётчик из `UserStatsStore` вместо суммы ошибок (`accentErrors + wordBlankErrors`) — это было антимотивирующее анти-достижение. Импорты AccentErrorsStore/WordBlankErrorsStore из StatsScreen удалены. **(2)** `runRequest` в `ai/Providers.kt` принимает `providerLabel`. 5xx → «$providerLabel временно недоступен (ошибка N). Попробуй через пару минут или переключись на другой провайдер»; JSONException в 200..299 → «Неожиданный ответ от сервера $providerLabel» (без HTML-снiппета); 4xx прочие → код без HTML. Каждый из 3 провайдеров (OpenRouter/Gemini/Anthropic) передаёт свой label. **(3)** Backup `CURRENT_VERSION = "1.6"`, `SUPPORTED_VERSIONS = ["1.0".."1.6"]`. Поле `UserStatsSnapshot.trainerWordsLearned: Int = 0` опциональное — старые v1.0-v1.5 парсятся OK. **Compile fixes по дороге:** (1) `const val EGE100_SYSTEM_PROMPT` не может содержать `trimIndent()` (runtime call) → `val`; (2) в обоих тренажёрах добавлены `import androidx.compose.runtime.{mutableStateOf, remember, setValue}` для `var showAi by remember { mutableStateOf(false) }` delegate. **APK 230.8 MB** (без роста к Stage A+B — все правки в коде). Conventions #48-54 зафиксированы. **Phase 4 НЕ закрыта** — открыт Stage C2 с 4 known issues: (1) auto-advance тренажёра при открытом AI окне; (2) путаница типов AI-лимитов (приложенческий vs провайдерский); (3) тренажёры №9-12 без кнопок букв (только клавиатура); (4) диагностика краша приложения после P4-C-теста.

2026-05-19 — **Phase 4 Stage A+B ✅ DONE** (теги `phase-4-stage-a-done` + `phase-4-stage-b-done`). Большая объединённая итерация: AI-помощник (3 провайдера, EncryptedSharedPreferences, кеш ответов, дневной лимит) + раздельные пробники по предметам (math 19 / rus 26) + 6 эталонных вариантов КИМ ФИПИ + история пробников с TrendChart. Плюс **3 quick fixes по итогам теста на Samsung** перед коммитом. **Часть A1-A2 (Routes + DB):** новые `ai/AiProvider.kt`, `ai/Providers.kt`, `ai/AiProviderRegistry.kt` (Convention #38). Зависимости: `okhttp 4.12.0` + `security-crypto 1.1.0-alpha06`. UserDataDatabase v2→v3→v4 (Migration_2_3 разбила старые `mathCorrect/Total/rusCorrect/Total` на отдельные строки per subject; Migration_3_4 добавила `ai_response_cache`). Routes расширены: `MockExamRunnerRoute(subject = "math", fipiVariantId = null)`, `FipiVariantsRoute`, `MockExamHistoryRoute`. **Часть A3 (3 провайдера):** OpenRouterProvider (sk-or-v1-..., default `openrouter/free`), GeminiProvider (AIza..., default `gemini-2.0-flash`), AnthropicProvider (sk-ant-api03-..., default `claude-sonnet-4-6`). Каждый через OkHttp + org.json (без kotlinx.serialization). Общий `runRequest` helper: 401/403→isAuthError, 404→«модель недоступна», 429→rate limit (Convention #46). EGE100_SYSTEM_PROMPT общий для всех. **Часть A4 (SecureKeyStore + AiSettings, Conventions #39-41):** `data/SecureKeyStore.kt` через `EncryptedSharedPreferences("ai_secure_keys")` — Tink-backed AES-256-SIV/AES-256-GCM. API: saveKey/getKey/hasKey/removeKey/clearAll. Исключён из 3 видов бэкапа (Convention #40). `data/AiSettingsStore.kt` — отдельный DataStore с auto-reset дневного счётчика по `LocalDate.now()`. **Часть A5 (UI):** `ui/ai/AskAiBottomSheet.kt` со sha256 кеш-ключом, daily limit check, FlowRow быстрых вопросов («Объясни решение», «Где ошибка?» с substitution userAnswer, «Какая формула?», «Похожий пример»). `ui/profile/AiSettingsSheets.kt` — 4 sheet'а (Провайдер/Модель с FREE badge/API ключ с 👁/🙈 toggle/Daily limit с пресетами 10/25/50/100/200). `SettingsScreen.kt` — секция «AI помощник». Кнопка «🤖 ИИ» в `ProblemDetailScreen` — Safety Rule #2: `enabled = st.checkResult !is CheckResult.Idle`. **Часть B1-B2 (раздельные пробники + FIPI, Conventions #42-44):** `MockExamRunnerViewModel.composeMath()` и `composeRus()` — по одной случайной задаче из каждого типа 1..19 / 1..26 (rus без сочинения №27). `SubjectChooserBottomSheet` при «Старт пробника». Старый `composeMix()` удалён. `parser/scrapers/parse_fipi_variants.py` — 6 эталонных вариантов (3 math + 3 rus) с фиксированным seed, в `parser/fipi_variants.json` + `assets/fipi_variants.json`. `FipiVariantsRepository`, `FipiVariantsScreen` с группировкой по предмету. `MockExamHistoryScreen` с `TrendCard` Canvas-chart (math синий / rus оранжевый / целевой балл пунктиром зелёным). `mock_exam_results.source` discriminator (`"internal"` / `"fipi"`), FIPI-результаты с `planIndex = -1` + `scheduledDate = fipiVariantId`. **Часть B3 (Главный экран + Backup v1.5):** `ui/home/QuickActionsCard.kt` — «🚀 Быстрый старт» с 3 ListRow (Пробник Math / Пробник Rus / Варианты КИМ ФИПИ). BackupSnapshot v1.5 (`SUPPORTED_VERSIONS = ["1.0".."1.5"]`): добавлены `aiSettings: AiSettings` (без ключей!) и mock_exam_results теперь с полями `subject`/`source`. `resetProgress` чистит и SecureKeyStore (явный wipe ключей). **3 quick fixes по итогам Samsung-теста:** (1) **Convention #45 — INTERNET permission**: до Phase 4 приложение было офлайн, добавлены `<uses-permission android:name="android.permission.INTERNET" />` + `ACCESS_NETWORK_STATE` после POST_NOTIFICATIONS — иначе OkHttp бросает SecurityException «Permission denied». (2) **Convention #46 — OpenRouter free models rotation**: `deepseek/deepseek-r1:free` 404'нул в актуальном каталоге OpenRouter — поменян default на `openrouter/free` auto-router (сам выбирает доступную бесплатную модель из текущего каталога). Остальные конкретные free-модели оставлены fallback'ом. Добавлен 404-кейс в runRequest. (3) **Convention #47 — AI context fix через Jsoup**: `stripHtmlForAi` старая regex-версия `<[^>]+>` уничтожала `<img alt="дробь: ...">` — формулы исчезали из контекста и AI отвечал наугад. Переписан через `Jsoup.parse(html).select("img[alt]").forEach { replaceWith(TextNode(" $alt ")) }` — alt вшивается inline. Лимит 2000→4000 символов с обрезкой по концу предложения. Защита от пустого context в `AskAiViewModel.ask`. `Log.d("AskAi", ...)` для диагностики. EGE100_SYSTEM_PROMPT усилен («формулы расшифрованы alt-текстом в скобках»). **APK 230.8 MB** (+1.3 MB к Phase 3 — основной рост от 6 FIPI-вариантов в assets + AI-зависимостей). Schemas `2.json`, `3.json`, `4.json` в `android/app/schemas/com.daniel.ege100.data.UserDataDatabase/` коммитим. Conventions #38-47 зафиксированы. Stage P4-C (финальная полировка, 5-7 часов, 8 пунктов) — следующая итерация.

2026-05-19 — **Phase 3 ✅ DONE** (теги `phase-3-stage-d-done` + `phase-3-done`). Финальная итерация Stage P3-FINAL — 6 частей в одной ночной сессии. **Часть А (Календарь пробников, Convention #33):** `data/MockExamSchedule.kt` хранит только install_date в DataStore, остальное считает на лету: первый пробник `max(install+28, today+14)`, дальше +21 день, ≤ examDate-7, максимум 16. `MockExamResultEntity` + Dao + UserDataDatabase v1→v2 + Migration с DDL для mock_exam_results. `ui/mock/MockExamCalendarScreen.kt` со списком статусов UPCOMING/TODAY/PAST + SummaryCard. `MockExamDetailScreen.kt` — превью / результат прошлого + «Перепройти». `MockExamRunnerScreen.kt` с `composeMix()` (8 math + 8 rus, шафл), AnimatedContent slide-spring, VerdictBanner, FinishedScreen с прогнозом ФИПИ-балла (FipiScoreTable.rawToTest), сохранение в `mock_exam_results` + attempt_log с `source = "mock_exam"`. **Часть Б (WorkManager + push, Convention #34):** work-runtime-ktx 2.10.0. `notifications/NotificationHelper.kt` с каналом `ege100_main`, проверкой POST_NOTIFICATIONS на Android 13+, 3 notification IDs. `ReminderWorkers.kt` — StreakReminderWorker (20:00, push при solved in 1..9), DailyReminderWorker (9:00, push при gap >= 2 дня от lastActive), MockExamReminderWorker (9:00, push если завтра пробник). Каждый воркер в начале doWork читает соответствующий `AppSettings.notifyXxx` флаг — если выключено, success без действий (паттерн gating). `EgeApplication.onCreate` регистрирует все 3 через `enqueueUniquePeriodicWork(KEEP)` с `setInitialDelay(delayUntil(hour, minute))`. `AndroidManifest.xml` + `<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />`. `MainActivity.requestNotificationPermissionIfNeeded()` через `ActivityResultContracts.RequestPermission()`. **Часть В (SafetyGuards #5 + #6, Convention #35):** `AttemptLogDao.getCountBetween(startMs, endMs)`. `data/SafetyGuardsStore.kt` хранит флаги + кэш счётчиков + lastCheckedEightWeekPeriod. `SafetyGuardsChecker.checkWeekly()` запускается в HomeViewModel.init: считает понедельник-воскресенье прошлой недели, при <50 активирует WeeklyGuardCard под шапкой Главного (SystemRedTint фон). `checkEightWeek()` каждые 8 недель от install_date: при <300 показывает EightWeekCheckpointDialog **один раз** (защита через lastCheckedEightWeekPeriod). После confirm — dismissEightWeek снимает флаг до следующего периода. Это политические инструменты, не блокировки. **Часть Г (Backup v1.3, Convention #36):** version «1.3», SUPPORTED_VERSIONS = `["1.0", "1.1", "1.2", "1.3"]`. Добавлены `mockExamResults: List<MockExamResultRecord> = emptyList()`, `installDate: String? = null`, `guardsState: GuardsState = GuardsState()`. exportBackup собирает через MockExamResultDao.getAll + MockExamSchedule.peekInstallDate + SafetyGuardsStore.snapshot. applyBackup: mockDao.deleteAll() + restoreInstallDate + SafetyGuardsStore.restore. **resetProgress сохраняет install_date** (техническая дата, не пользовательская), но чистит mock_exam_results + safety_guards. **Часть Д (typesCovered fix, Convention #37):** StatsViewModel.typesCovered = `count { attempts >= 15 && accuracy >= 0.70f }` (раньше attempts > 0 — ложная мотивация после 1 задачи). AchievementRow получил `hint: String?` и `accent: Boolean` — для «Освоено типов» hint = «15+ решений · точность 70%+» серым под title, accent=false при 0 → SystemBlue→LabelSecondary. TypeAccuracyRow показывает ✓ SystemGreen рядом с типом если mastered. **Часть Е (полировка):** LightSystemBlue 0xFF007AFF → 0xFF0066CC (контраст в светлой теме), PressableSurface scale 0.97 → 0.96 (заметнее тап), `WelcomeCard` для новых пользователей (totalAttempts == 0) на Главном, `PreparationProgressCard` в ProfileScreen (решено всего / дней до ЕГЭ / пробников пройдено через LaunchedEffect загрузку). **HomeViewModel** расширен: combine теперь 4 потока (profile + streak + settings + guards), SafetyGuardsChecker.checkWeekly/checkEightWeek в init, `daysUntilNextMock` через `MockExamSchedule.getDaysUntilNext()` вместо хардкода 28. **Routes:** MockExamCalendarRoute / MockExamDetailRoute(planIndex) / MockExamRunnerRoute(planIndex). MockExamPreviewCard на Главном теперь активна, ведёт в календарь. **APK 229.5 MB** (без роста к Stage C2 — почти всё новое в коде). Schema `2.json` в `android/app/schemas/com.daniel.ege100.data.UserDataDatabase/` коммитим. Conventions #33-37 зафиксированы. **Phase 3 закрыта полностью.** Известная правка отложена в P4-A: пробник через выбор предмета (math 19 / rus 27) вместо смешанных 16.

2026-05-18 — **Phase 3 Stage C2 ✅ DONE** (тег `phase-3-stage-c2-done`). Мини-стейдж: 3 правки после теста Stage C. **Правка 1 (Convention #31, спойлер):** в `ErrorsListScreen.kt` старый `AnswerComparison` (одна Row с «Твой: X → Верно: Y») заменён на `AnswerBlock(userAnswer, correctAnswer, revealed, onReveal)`. Правильный ответ скрыт по умолчанию — пользователь видит «Твой: X» красным и ссылку-кнопку «Показать правильный ответ» (SystemBlue 13sp). По тапу раскрывается «Верно: Y» зелёным. Для уже перерешённых задач (`isResolved == true`) ответ виден сразу. Ключ `remember(row.error.id)` — состояние раскрытия per-карточка, не теряется при LazyColumn recycling. **Правка 2 (опечатка):** «непререшённых» → «неперерешённых» (правильная форма от «перерешать»). Заменено в 4 местах: JournalScreen.kt (subtitle), ErrorsListScreen.kt (LargeTitleBar subtitle + ErrorFilter.UNRESOLVED label), ProblemDetailScreen.kt (комментарий). **Правка 3 (Convention #32, NotEnoughDataHint):** в `ui/home/RadarCard.kt` проверка `coloredCount = stats.count { it.severity != Severity.GRAY }` поднята на уровень RadarCard до switch по style. Если `coloredCount == 0` — единый `NotEnoughDataHint(stats)` вместо разных EmptyHint в 4 стилях. Текст: «Радар появится, когда хотя бы в одном подвиде накопится 15+ попыток» + live-прогресс «Сейчас: N попыток в M подвидах. Максимум в одном: K / 15». RadarHeatmap фильтр упрощён до `attempts > 0` (вместо странного `attempts > 0 || severity != GRAY`). Локальные EmptyHint вызовы во всех 4 рендерах удалены (туда попадаем только если `coloredCount > 0`). Старый `private fun EmptyHint()` helper удалён — больше не используется. APK 229.6 MB (+0.1 MB к Stage C). Conventions #31-32 зафиксированы.

2026-05-18 — **Phase 3 Stage C ✅ DONE** (тег `phase-3-stage-c-done`). Журнал из 3 stub'ов превращён в **аналитический центр** работы над ошибками + детальная статистика + экспорт сырых данных. 6 частей. **Часть А (БД-таблицы):** новая `data/UserDataDatabase.kt` (Room v1, отдельная от corpus.db — Convention #28). Две таблицы: `error_log` (problem_id, user_answer, correct_answer, timestamp, is_resolved) + `attempt_log` (problem_id?, subject, type_number, subtype_id?, is_correct, duration_ms, timestamp, source). FK к corpus.db невозможны (cross-database) — хранится только problem_id, целостность проверяется в ErrorsListViewModel.refresh через CatalogDao.getProblem. Hook (Convention #29) подключён в 3 ViewModel'и рядом с UserStatsStore.recordAttempt/StreakStore.onProblemSolved: ProblemDetailViewModel.check (attempt_log всегда + error_log при `!isCorrect && answer != null`; при `fromErrors && isCorrect` — `markAllResolvedFor(problem.id)`), AccentTrainerViewModel.tapSyllable (только attempt_log, source `accent_trainer`), WordBlankTrainerViewModel.check (только attempt_log, source `wordblank_trainer`). **Часть Б (ErrorsListScreen):** `ui/journal/ErrorsListScreen.kt` со списком ErrorRow (errorLog + problem из corpus.db). ErrorCard: TypeBadge (subject + №), timestamp (формат «d MMM, HH:mm» Locale.forLanguageTag("ru")), preview условия 140 chars без HTML, AnswerComparison (Твой красный → Верно зелёный), кнопки 🔁 Перерешать / ✓ markResolved / 🗑 delete. ErrorFilter — 5 вариантов (ALL/UNRESOLVED/LAST_WEEK/BY_TYPE_MATH/BY_TYPE_RUS) через FilterBottomSheet с RadioRow. EmptyState. ProblemDetailRoute расширен `fromErrors: Boolean = false` — в режиме «Перерешать» отключаются prev/next, timer durationMs от openedAtMs, при правильном ответе все непререшённые записи задачи помечаются resolved. **Часть В (JournalScreen v2):** переписан полностью — JournalSummaryCard сверху (сегодня N задач + точность M% + всего K) + 4 раздела (Избранные/Ошибки с unresolved count/Статистика/Экспорт CSV). JournalViewModel.refresh пересчитывает todaySolved/todayCorrect/totalSolved/favoritesCount/errorsCount/unresolvedErrors при `LaunchedEffect(Unit)`. **Часть Г (StatsScreen):** `ui/journal/StatsScreen.kt` — OverviewCard (total + correct + accuracy + avgDurationMs), ActivityChartCard (Canvas-столбцы за 30 дней с цветом по accuracy ≥80%→green, ≥60%→blue, иначе orange; добиваем пустые дни до полного окна 30 дней; даты «18.05» в подписях), TypeAccuracyTableCard (math 19 типов + rus 27 типов с прогресс-баром и долей correct/attempts, фильтр attempts > 0), AchievementsRow (maxStreak, typesCovered/totalTypes, wordsLearned, favoritesCount). `getDailyStats` через SQL `DATE(timestamp/1000, 'unixepoch', 'localtime')` group by. **Часть Д (CsvExporter, Convention #30):** `data/CsvExporter.kt` — формат 9 колонок `timestamp_ms,date,subject,type_number,subtype_id,problem_id,is_correct,duration_ms,source`, **UTF-8 с BOM** (критично для Excel — без BOM кириллица ломается), дата экранирована в кавычки, файл `cacheDir/ege100_attempts_<date>.csv`, share через `Intent.createChooser(ACTION_SEND, "text/csv")` + FileProvider (Convention #23). `triggerCsvExport()` в EgeApp.kt при тапе по «📤 Экспорт CSV» в Журнале. **Часть Е (Backup v1.2, Convention #27 расширение):** BackupSnapshot.CURRENT_VERSION = «1.2», SUPPORTED_VERSIONS = `["1.0", "1.1", "1.2"]`. Добавлены `errorLog: List<ErrorLogRecord> = emptyList()` + `attemptLog: List<AttemptLogRecord> = emptyList()`. `ErrorLogRecord`/`AttemptLogRecord` — сериализационные клоны Entity без autoGenerate id (`toEntity()`/`fromEntity()`). applyBackup: `errorDao.deleteAll()` + `attemptDao.deleteAll()` ПЕРЕД insert каждой записи — иначе autoGenerate id создаст дубликаты. resetProgress теперь чистит и `error_log` и `attempt_log`. **Routes:** `ErrorsListRoute`, `StatsRoute`, `ProblemDetailRoute.fromErrors`. `matchesRoot(JournalStubRoute)` расширен ErrorsListRoute/StatsRoute — таб «Журнал» остаётся активным при переходах внутрь. **Schema:** `android/app/schemas/com.daniel.ege100.data.UserDataDatabase/1.json` в git (для будущих миграций). **APK 229.5 MB** (+0.3 MB к Stage B — почти всё в коде). Conventions #28-30 зафиксированы. 3 правки отложены в P3-C2 (см. Known issues): спойлер ответов, опечатка «непререшённых» → «неперерешённых», диагностика смены варианта радара.

2026-05-18 — **Phase 3 Stage B ✅ DONE** (тег `phase-3-stage-b-done`). Главный экран превратился из заглушки в полноценный центр подготовки. 7 частей: цитата дня + шапка + streak + предиктор + радар (4 стиля) + быстрый тренажёр + карточка пробников. **Часть А (Цитаты):** `parser/scrapers/generate_quotes.py` со 153 цитатами (50 русских учёных + 50 зарубежных + 33 мыслителя + 20 о труде) — паттерн от Stage 5 rules.json. `QuotesRepository.getTodayQuote()` — детерминированно через `today.toEpochDay() % size` (одна цитата в день для всех, цикл ~150 дней). `QuoteCard` с кавычкой «❝» SystemBlue alpha 0.3 + italic + автор LabelSecondary. **Часть Б (Шапка):** `HomeHeader` 32sp Bold «Привет, $name!» + AvatarChip 44dp (тап→ProfileRoute) + Metric chips «🔥 N дней · 📅 N дней до ЕГЭ» с правильным склонением через `ui/common/Pluralize.kt` (общий helper `daysWord/problemsWord/attemptsWord`). Streak ≥7 → 🔥 SystemOrange. **Часть В (Streak):** `data/StreakStore.kt` с DAILY_GOAL=10. Поля: currentStreak/maxStreak/todaySolvedCount/lastActiveDate. `onProblemSolved()` подключён в **3 ViewModel** (Convention #24): ProblemDetailViewModel.check (только при isFirstCheck), AccentTrainerViewModel.tapSyllable (на Verdict), WordBlankTrainerViewModel.check. `checkValidity()` на старте Home обнуляет current если gap > 1 день (max сохраняется). **Часть Г (Предиктор):** `FipiScoreTable` зашит из приказа ФИПИ-2024 (math 0..32 raw, rus 0..50 raw). `UserStatsStore` (data/UserStatsStore.kt) — DataStore с ключами `type_total_<subject>_<n>` / `type_correct_*` / `subtype_total_<id>` / `subtype_correct_*`. `recordAttempt(subject, typeNumber, subtypeId, isCorrect)` в тех же 3 ViewModel. `ScorePredictor.predict()` (Convention #25): `basePercent` step-функция от accuracy, blend `mixed = base × coverage + 0.30 × (1-coverage)` против overconfident estimate, weighted by max raw per type. `PredictorCard` цветной (green/orange/red vs target), при confidence < 0.3 — подсказка. **Часть Д (Радар 4 стиля, Convention #26):** `data/SubtypeStatsRepository.kt` со Severity GRAY/RED/YELLOW/GREEN (порог 15 attempts из Safety Rule #1). `ui/home/RadarCard.kt` — `when (style)` LIST/DONUT/HEATMAP/RADAR_CHART. LIST: топ-15 не-серых + AppleProgressBar. DONUT: Canvas drawArc stroke 28dp + центр «N слабых» + легенда. HEATMAP: сетка 7×N плиток 38dp. RADAR_CHART: Canvas с 5-уровневой сеткой polygon + filled path SystemBlue alpha 0.25, агрегация 50+ подвидов в 7 крупных тем (Алгебра/Геометрия/Вероятность/Параметр и числа/Орфография/Пунктуация/Текст и лексика). **Часть Е (Быстрый тренажёр):** `SubtypeStatsRepository.composeWeakMix()` берёт топ-3 RED подвида (sorted by accuracy ASC) → 4 random задач каждый → take(10); fallback на подвиды с attempts<15 если RED нет. `CatalogDao.getRandomProblemsInSubtype()` + `getSubtypesBySubject()` + `getSubjectBySlug()`. `QuickTrainerRoute(problemIdsCsv)` (Navigation Compose 2.8 не сериализует List<Long> в URL напрямую — CSV-обходом). `ui/quick/QuickTrainerScreen.kt` переиспользует ProblemDetailScreen, AnimatedContent с fadeIn+scaleIn spring 0.85, CompletionState с «✓ Готово!». **Часть Ж (Пробники + Backup):** `MockExamPreviewCard` с расчётом «первый через 28 дней, дальше каждые 21» (полный календарь+WorkManager в P3-D). `BackupSnapshot` бамп до версии 1.1 (Convention #27): добавлены `userStats: UserStatsSnapshot` и `streak: StreakState` с default-значениями. `SUPPORTED_VERSIONS = ["1.0", "1.1"]` — старые бэкапы парсятся OK. `resetProgress()` теперь чистит stats + streak. **HomeViewModel** через `combine(profileFlow, streakFlow, settingsFlow)` для reactive части + `LaunchedEffect(Unit) { refresh() }` для computed (quote/predictor/stats). Удалён `StubScreens.kt` (HomeStubScreen больше не используется). **APK 229 MB** (без роста к Stage A — весь рост в коде). Conventions #24-27 зафиксированы.

2026-05-18 — **Phase 3 Stage A ✅ DONE** (тег `phase-3-stage-a-done`). Первая итерация Phase 3 — 5 частей: polish-долги из P2 + 4-й таб «Профиль» + экран Настройки с мгновенной сменой темы + экспорт/импорт прогресса через системный share-sheet. **Часть А (Polish):** новый `ui/common/AppleProgressBar.kt` (6dp capsule, track `BgElevated2` динамический — видим в обеих темах, spring NoBouncy + StiffnessMediumLow). Свайпы в AccentTrainer/WordBlankTrainer: `dampingRatio = 0.85f` + `StiffnessMediumLow` + fade 280ms (было 0.75f / Medium / 220ms — стало плавнее, «как iOS»). `LargeTitleBar`/`SmallTitleBar` обёрнуты в `windowInsetsPadding(WindowInsets.statusBars)` — стрелка ← и ⇆ chip в шапке тренажёров больше не прижимаются к status bar (общий фикс для всех topBar'ов). **Часть Б (4-й таб):** `ProfileRoute` + `SettingsRoute` в Routes.kt. `BottomTabBar` расширен до 4 табов (Главная/Решать/Журнал/Профиль), иконки 26→24sp, padding 8→4dp. `HomeStubScreen` получил `AvatarChip` 36dp в `rightContent` slot (инициал имени или 👤, тап → ProfileRoute — двойной доступ к профилю). **Часть В (Профиль):** `data/UserProfileStore.kt` — DataStore с полями `name`/`birthDate`/`targetScore`/`examDate` (дефолт `2027-06-04` — рекомендуемая ФИПИ-дата). `UserProfile.daysUntilExam(today)` с правильным склонением день/дня/дней. `ui/profile/ProfileScreen.kt` — header (аватар 80dp + имя + «До ЕГЭ-2027 осталось N дней») + 3 секции AppleListRow (Личные данные / Подготовка / О приложении). `ui/profile/EditBottomSheets.kt`: `EditNameBottomSheet` (IosTextField), `EditScoreBottomSheet` (4 stepper-кнопки ±1/±5 диапазон 50-100), `EditDateBottomSheet` (3 wheel-колонки день/месяц/год с тап-выбором без snap-логики). **Часть Г (Настройки + смена темы):** `data/AppSettingsStore.kt` — DataStore с `ThemeMode` (AUTO/DARK/LIGHT), `RadarStyle` (LIST/DONUT/HEATMAP/RADAR_CHART), 3 notify booleans. Дефолты: AUTO, LIST, все ON. В `Color.kt` добавлен `LocalDarkOverride: staticCompositionLocalOf<Boolean?>` — все color getters читают override → fallback `isSystemInDarkTheme()`. `EgeTheme(themeMode)` оборачивает в `CompositionLocalProvider(LocalDarkOverride provides useDark)`. `MainActivity` подписан на `AppSettingsStore.settingsFlow.collectAsState` → мгновенная перекраска при смене темы без перезапуска Activity (Convention #21). `SettingsScreen.kt` — 3 секции (Внешний вид / Уведомления / Данные), `ThemeBottomSheet` и `RadarBottomSheet` с RadioRow (CircleShape + border 1.5dp + checkmark), 3 Material3 Switch (SystemBlue track, white thumb). **Часть Д (Бэкап):** `data/BackupRepository.kt` — `exportBackup()` собирает `BackupSnapshot(version=1.0, exportedAt ISO Instant, appVersion, profile, settings, trainerProgress, favorites, accentErrors, wordBlankErrors)`. `parseBackup()` валидирует version, `applyBackup()` восстанавливает все Store'ы (Convention #22). `resetProgress()` чистит Trainer/Favorites/Errors, оставляя Profile/Settings. `data/BackupShare.kt` — `buildShareIntent()` пишет JSON в `cacheDir/ege100_backup_<date>.json` через `FileProvider.getUriForFile()`, готовит `Intent.createChooser(ACTION_SEND, "application/json")` (Convention #23). `readUriContent()` для импорта через `contentResolver.openInputStream`. `res/xml/file_paths.xml` (FileProvider) + `backup_rules.xml` (Android 11-) + `data_extraction_rules.xml` (Android 12+) с **исключением `corpus.db`** (192 MB вне лимитов Auto Backup). `AndroidManifest.xml`: `allowBackup="true"` + `dataExtractionRules` + `fullBackupContent` + `<provider androidx.core.content.FileProvider>`. `EgeApp.kt` подключает `ActivityResultContracts.GetContent("application/json")` launcher для импорта, `ImportConfirmBottomSheet` с датой бэкапа → applyBackup → Toast, `ResetProgressBottomSheet` с `DangerButton` → resetProgress → Toast. Расширены все Store'ы триадой `snapshot`/`restore`/`clearAll`: `TrainerProgressStore.getAllProgress/restoreAll/clearAll`, `AccentErrorsStore.restore/clearAll`, `WordBlankErrorsStore.getAll/restore/clearAll`, `FavoritesStore.restore/clearAll`, `UserProfileStore.restore`, `AppSettingsStore.restore`. **APK 229 MB** (+0.5 MB к Stage 5). 19 smoke-тестов пройдены (включая экспорт в Telegram Saved Messages → импорт обратно через Telegram → восстановление прогресса). Conventions #21-23 зафиксированы.

2026-05-18 — **Phase 2 ✅ DONE** (закрытие всей фазы — теги `phase-2-stage-5-done` + `phase-2-done`). Финальная итерация Stage 5 — 8 частей в одном коммите. **Часть А (Память тренажёров):** `data/TrainerProgressStore.kt` — DataStore с JSON-снэпшотом `{position, total, order, indices}` под ключом `progress_<trainerId>` (12 trainerId: 8 accent + 4 blank). Двухстадийный `ResumeBottomSheet` (ui/common): «Продолжить» (PrimaryButton) → восстановление, «Начать сначала» → второй sheet с `DangerButton` (новый компонент в AppleButton.kt, красный CTA) → `clearProgress()`. `persistProgress()` в каждом `goNext`/`goPrev`, `clearProgress()` при достижении последнего слова и при `toggleOrder()`. Применено к AccentTrainerScreen + WordBlankTrainerScreen. Convention #18. **Часть Б (Edge swipe back):** `ui/modifiers/EdgeSwipeBack.kt` — `Modifier.edgeSwipeBack` слушает `PointerEventPass.Initial`, реагирует только если `down.x < 24dp` и `dragX > 100dp`, не consume событий → не ломает LazyColumn-прокрутку. Применён один раз на `NavHost.modifier`. BackButton в `Scaffolds.kt` высота 56dp + `Alignment.BottomCenter` + bottom padding — стрелка визуально опущена ближе к большому заголовку. **Часть В (46 правил):** `parser/scrapers/generate_rules.py` — Python-словарь с 19 math + 27 rus правил (76K символов, avg 1660 chars = ~250 слов на правило, формат Markdown с `## ### -` и `**жирный**`). Никакого Claude API — все тексты вручную из знаний школьной программы 10-11 класса. Выход: `parser/rules.json` (139 KB, в git) + копия `android/app/src/main/assets/rules.json` (gitignored). Convention #19. **Часть Г (Кнопка «Правило»):** `data/RulesRepository.kt` (кеш в памяти, маппинг `mathb` → `math_N`, `rus` → `rus_N`) + `ui/common/SimpleMarkdownRenderer.kt` (рендер ## ### списков буллетов нумерованных **жирного**, ~150 строк, без external deps) + `ui/common/RuleBottomSheet.kt` (ModalBottomSheet 85% высоты, BgElevated, скролл). Кнопка «📋 Правило» в `ProblemDetailScreen` enabled только при `st.rule != null`. **Часть Д (Избранное):** `data/FavoritesStore.kt` (DataStore Preferences с Set<String> problem_id'ов), звезда в шапке `ProblemDetailScreen` (☆/★, bounce через `animateFloatAsState` + spring DampingRatioMediumBouncy, haptic LongPress). `data/CatalogDao.kt` — `getProblemsByIds(ids: List<Long>)` для подгрузки избранных. JournalStubScreen заменён на `ui/journal/JournalScreen.kt` (3 раздела: ⭐ Избранные с счётчиком, 📝 Ошибки stub, 📊 Статистика stub) + `ui/journal/FavoritesScreen.kt` (подписка на FavoritesStore через collectLatest → автообновление списка). **Часть Е (Свайпы между задачами):** `pointerInput` на LazyColumn-modifier в `ProblemDetailScreen` с `detectHorizontalDragGestures`, edge guard `skip = startX < 24.dp.toPx()` (координация с edgeSwipeBack), threshold 90dp. Кнопки PrevNextBar оставлены. Convention #20. **Часть Ж (Светлая тема):** `ui/theme/Color.kt` полностью переписан — все цвета палитры теперь top-level `val Bg: Color @Composable @ReadOnlyComposable get()` с проверкой `isSystemInDarkTheme()`. Два набора hardcoded цветов: `DarkBg/DarkLabel/...` и `LightBg/LightLabel/...` (iOS light палитра: `#FFFFFF`/`#F2F2F7`/`#E5E5EA` фоны, `#007AFF` синий чуть темнее). `Theme.kt` собирает `darkColorScheme()` или `lightColorScheme()` динамически. `MainActivity` убран принудительный `darkTheme = true`. В `HtmlRenderer` luminance inversion применяется только при `isSystemInDarkTheme() == true` → в светлой теме формулы остаются чёрными на белом. **Часть З (Иконка):** `res/drawable/ic_launcher_foreground.xml` — концепт «Геометрия»: белый filled треугольник (vertices 54,30 → 26,78 → 82,78) + синяя `#0A84FF` filled окружность (центр 54,62, радиус 14, ~ инцентр треугольника) на синем background `#0A84FF`. Monochrome для Android 13+ themed icons — тот же path. Round вариант — тот же XML, маска применяется системой. **Итого:** 13 файлов изменено + 10 файлов создано в `data/`, `ui/common/`, `ui/journal/`, `ui/modifiers/`. APK **228 MB** (+9 MB к Stage 4). 3 мелких недочёта отложены в Phase 3 (см. «Known issues» в Roadmap). Теги `phase-2-stage-5-done` + `phase-2-done` (закрытие всей Phase 2). Conventions #18-20 зафиксированы.

2026-05-18 — **Phase 2 Stage 4 ✅ DONE** (объединённая итерация Polish 4 + тренажёры русского №9-12). Часть А: HtmlRenderer разделён на `BlockFormula` (cap 120dp, wrapContentSize по `readSvgSize`) + `BlockIllustration` (fillMaxWidth + 360dp + border) — простые формулы (`√3`) больше не растягиваются на 360dp; FlowRow в AccentTrainer с адаптивным шрифтом (32/28/24sp по числу слогов) — «кровоточащий» (5 слогов) и «вероисповедание» (8 слогов) помещаются. Часть Б: `parser/scrapers/extract_word_blanks.py` парсит 541 задачу №9-12 русского из corpus.db; распознаёт оба формата sdamgia bold-разметки (`<b>загорелый</b>` целиком и `<span class="root">р<b>а</b>ст</span>ительность` ответ-буквой) → 4089/6719 (60.9%) пропусков → **2674 уникальных слов** (847 №9 Корни + 715 №10 Приставки + 517 №11 Суффиксы + 595 №12 Окончания/Причастия) в `parser/word_blanks.json` (807 KB). Новый UI: `WordBlanks.kt` (Repository + DataStore ErrorsStore), `WordBlankTrainerScreen.kt` со state machine `Empty → Verdict(userAnswer, correctAnswer, isRight)`, IosTextField (только русские буквы, 1-3 символа), Primary «Проверить», подсветка ответа в полном слове, rule_hint, свайпы влево/вправо, авто-переход через 1 сек при правильном. Карточки тренажёров (🌱🧱🎀🌀) в SubtypesScreen для типов 9-12 русского. APK 219 MB. Тег `phase-2-stage-4-done`.

2026-05-18 — **Phase 2 Stage 3 ✅ DONE** (Polish 1 + 2 + 3). Polish 1: iOS-палитра (Color.kt), Inter через Google Fonts (Type.kt + fonts_certs.xml), `AppleButton/AppleCard/AppleListRow/IosTextField/LargeTitleBar` UI-система через `PressableSurface` (Convention #15). SolutionEntity в Room v1→2 + Migration. ProblemDetailScreen с большой шапкой position/total, IosTextField, Verdict-карточкой со scaleIn 0.5→1. Тренажёр ударений №4: 230 слов в 6 категориях ФИПИ (accent_words.json), AccentCategoriesScreen с 6 категориями + «🎲 Все слова» + «🔤 Все по алфавиту», AccentTrainerScreen со слогами-кнопками + свайпами, `syllabify` алгоритм, AccentErrorsStore (DataStore). Polish 2: `parser/scrapers/fix_html_image_refs.py` чинит 3126 mismatch-расширений (.png → реальное) + удаляет 28471 UI-иконку sdamgia + 482 absolute formula URL → `_formulas/` (Convention #13). Подключён `parser/assets/` как второй sourceSet, AAPT2 `ignoreAssetsPattern` переопределён без `_*` mask, corpus.db больше не `noCompress`. Polish 3: попиксельный `applyLuminanceInversion` с `maxDiff<30` для тёмной темы (Convention #14) — чёрные линии чертежа белеют, цветные элементы (оранжевая окружность) сохраняют hue. `SyllableTapState` (тап на слог = выбор, без раскрытия). NavHost spring-slide анимации с параллакс-эффектом + fade для tab-switching. Скругления 20→24dp / 14→16dp / 12→14dp. Back-button 48dp тап-зона. Шрифт условия 17→18sp. APK 218 MB. Тег `phase-2-stage-3-done`.

2026-05-17 — **Phase 2 Stage 2 ✅ DONE** (после фикса schema mismatch). Полноценный Room + Catalog flow: 4 @Entity (Subject, ProblemType, ProblemSubtype, Problem) под DDL `build_db.py` (FK, индексы `idx_problems_subject/type/subtype`, `idx_subtypes_sdamgia`, `defaultValue="0"` для `is_supplementary`). `CatalogDao` со suspend-запросами + COUNT-агрегаты на стороне SQL. Ручное копирование заменено на `Room.databaseBuilder.createFromAsset("corpus.db")`. Подключён kotlinx.serialization для type-safe Navigation Compose 2.8 (@Serializable routes: `CatalogRoute`, `TypesRoute`, `SubtypesRoute`, `ProblemListRoute(typeId, subtypeId?)`, `ProblemDetailRoute`). 5 экранов каталога: CatalogScreen (2 предмета 📐/✍️) → TypesScreen (19/27 типов) → SubtypesScreen (🎯 «все задачи типа» + список подвидов) → ProblemListScreen (LIMIT 50 + «Загрузить ещё») → ProblemDetailScreen (заглушка с сырым HTML + метаданными). Bottom-bar 3 таба (Главная/Решать/Журнал), стартовая destination = Решать. APK 204 MB. `exportSchema = true`, JSON в `android/app/schemas/com.daniel.ege100.data.EgeDatabase/1.json` коммитим в git для будущих миграций.

**Фикс по дороге (инцидент 2026-05-17): inline `INTEGER PRIMARY KEY` mismatch.** Первая попытка билда падала на старте через `IllegalStateException: Pre-packaged database has an invalid schema`. Диагностика через `exportSchema=true` показала: Room генерит `id INTEGER NOT NULL, ..., PRIMARY KEY(id)` (`PRAGMA notnull=1`), а наш `parser/build_db.py` писал inline `id INTEGER PRIMARY KEY` (rowid-alias форма, `PRAGMA notnull=0`). Room сравнивает `Column.notNull` напрямую без скидки на PK, не матчит. Исправлено: `SCHEMA_SQL` переписан на table-level PK для всех 11 таблиц с inline PRIMARY KEY (subjects, problem_types, problem_subtypes, problems, solutions, rules, user_progress, ai_conversations, error_atoms, mock_exams, daily_streak — последняя с `date TEXT NOT NULL, PRIMARY KEY(date)`). `problem_rules` уже имела table-level композитный PK, `problems_fts` — virtual table, валидация Room не применяется. После перегенерации corpus.db (192.27 MB, 12.5 сек, integrity_check ok) — `Room.createFromAsset` проходит. Конвенция #12 в «Конвенции парсера» зафиксировала это требование на будущее.

2026-05-17 — **Phase 2 Stage 1 ✅ DONE.** Создан Android-проект `android/` с пакетом `com.daniel.ege100` (имя на иконке — `EGE100`). Стек: Kotlin 2.0.21 + Compose BOM 2024.12.01 + Material 3 + Room 2.6.1 + Coil 2.7.0 + Navigation 2.8.5 + Ktor 3.0.3 (пустой). AGP 8.7.3, Gradle 8.11.1, JBR 21 от Android Studio. corpus.db (192 MB) лежит в `app/src/main/assets/` (gitignored), подключается через ручное копирование в databases/ + raw SQLite — Room.createFromAsset отложен до Stage 2, когда напишем @Entity под существующую схему. APK собран: 204 MB, в `android/app/build/outputs/apk/debug/app-debug.apk`. UI: одна Activity, тёмная тема, центрированный текст «БД подключена / Задач в корпусе: 10272». Smoke-тест на устройстве — на стороне пользователя (Samsung Galaxy через Telegram). Тег `phase-2-stage-1-done`.

2026-05-17 — **Phase 1 ✅ DONE.** Stage 4 закрыт: стресс-тест по КИМ ФИПИ-2026 показал 100% покрытие (46/46 задач: 19 math + 27 rus) через FTS5-поиск. Claude API не понадобился, бюджет $0. Тег `phase-1-stage-4-done`. Скрипт `parser/scrapers/stage4_coverage.py` — извлекает задачи из 2-колоночных PDF демоверсий ФИПИ, делит по «ИЛИ»-вариантам, матчит в corpus.db. Артефакт `parser/coverage_report.md` (104 KB, gitignored) с детализацией по каждой задаче. Roadmap: Фаза 1 ✅, Фаза 2 (Android MVP по `DESIGN_SPEC.md`) ready.

2026-05-17 — Косметика post-Stage 3 (после ручной проверки view_corpus.html). Два бага исправлены без re-scrape:
- 89% иллюстраций были SVG под именем `.png` (3128 из 3510 файлов) — добавлен `parser/scrapers/fix_illustration_extensions.py`, переименовал файлы и точечно обновил corpus.db (2637 + 688 + 2063 UPDATE на images_json/statement_html/solution_html). На уровне конвенций добавлено правило #10.
- HTML sdamgia отдаёт блоки с inline `display:none` — `parser/export_view.py` теперь прогоняет через `strip_display_none()`. На уровне конвенций добавлено правило #11 — обязательное для всех будущих UI (включая Android WebView).

2026-05-17 — Stage 3 закрыт. Создан `parser/build_db.py` — собирает `parser/corpus.db` (192 MB) из math.jsonl + russian.jsonl + russian_rules.jsonl + russian_problem_meta.jsonl + parser/assets/. Все 10272 задачи в `problems`, FTS5-индекс готов, 0 orphan записей, integrity_check ok. Создан `parser/export_view.py` — генерирует `parser/view_corpus.html` (10 math + 10 rus задач для визуальной проверки). Минорное отклонение от изначальной схемы: добавлен `subject_id` в `problems` и UNIQUE через пару `(subject_id, sdamgia_id)` — 9 коллизий ID между math и rus (историческое наследие sdamgia). Тег `phase-1-stage-3-done`.

2026-05-17 — Stage 2 закрыт. 5409 задач русского №1..№27 (100% покрытие main) в `parser/russian.jsonl`, 36 уникальных правил в `parser/russian_rules.jsonl`, per-problem метаданные (источник/сложность/rule_hash) в `parser/russian_problem_meta.jsonl`. Тег `phase-1-stage-2-done`. 0 ошибок, 0 банов, 0 4xx-5xx. Изменения в схеме БД: добавлены таблицы `rules` + `problem_rules` (many-to-many), колонки `problems.source` и `problems.difficulty` (только для русского). Math rules — user-curated, не парсятся из sdamgia. Парсер `parse_catalog` теперь автоматически детектирует разделитель `<h3>Дополнительные задания для подготовки</h3>` и помечает всё после него как supplementary — работает для обоих предметов. Smoke 32/32 зелёные.

2026-05-17 — Stage 1 закрыт. 4863 задачи математики (100% покрытие main №1..№19) лежат в `parser/math.jsonl`, формулы и иллюстрации в `parser/assets/`. Тег `phase-1-stage-1-done`. 0 ошибок, 0 банов. Добавлена конвенция #9 в «Конвенции парсера» (не использовать bash `&` для запуска парсера — только `run_in_background`). Подвиды Д1..Д19 (~2986 задач) отложены до Stage 4 по решению пользователя. Stage 2 (русский) ожидает мини-разведки от пользователя.

2026-05-16 — Stage 0 разведки sdamgia закрыт. Обновлены: схема `problem_types` (добавлен `is_supplementary` для «Дополнительных заданий Д1..Д14»), Safety Rule #1 (примечание про русский — радар по `problem_types.number`), Tech stack (формулы — SVG, не MathML), Roadmap (Stage 0 ✅, Stage 1 ready). Создан `parser/selectors.yaml`.
